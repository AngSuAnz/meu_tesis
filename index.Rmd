---
title: "Metodología de investigación"
description: |
  Caso de estudio: Barrio Padre Carlos Mugica y su entorno formal
---

  
```{r librerias, include=FALSE}

options(scipen = 999)

library(distill)
library(dplyr)
library(DT)
library(effsize)
library(ggmap)
library(ggplot2)
library(gotop)
library(htmltools)
library(janitor)
library(knitr)
library(leaflet)
library(leaflet.extras)
library(lwgeom)
library(osmdata)
library(osrm)
library(purrr)
library(readxl)
library(rmarkdown)
library(sf)
library(stringr)
library(tidyr)
library(tidyverse)

#In RStudio, you can render your site locally (knit + preview all .Rmd files in one fell swoop). Se puede escribir en la consola: rmarkdown::render_site()  SIEMPRE HACER ESTO PARA IMPACTAR LOS CAMBIOS EN EL SITIO!

```

```{r exclude_false, include=FALSE}
#PARA OCULTAR CHUNKS DE CODIGO
knitr::opts_chunk$set(
  comment =FALSE,
  #fig.width = 6, fig.height = 6, 
  message=FALSE, warning=FALSE
)
```

```{r, include=FALSE}
#SIMBOLO PARA SUBIR HASTA ARRIBA EN EL SITIO
gotop::use_gotop(
  src = "fas fa-chevron-circle-up", # css class from Font Awesome
  color = "tomato", # color
  opacity = 0.8, # transparency
  width = 30, # size
  appear = 100 # number of pixels before appearance
  )
```
<script>
function initTableSettings() {
  $('.searchable-table').each(function () {  // Only apply to tables with "searchable-table" class
    var table = $(this).DataTable();
    table.columns().every(function () {
      var column = this;
      var header = $(column.header());
      var input = $('<br><input type="text" placeholder="Filter" style="width: 75%;"/>').appendTo(header).on('keyup change', function () {
        if (column.search() !== this.value) {
          column.search(this.value).draw();
        }
      });
    });
  });
}

$(document).ready(function() {
  initTableSettings();
});
</script>

En la Ciudad Autónoma de Buenos Aires, las villas y asentamientos informales presentan distintos grados de integración al tejido urbano formal. Mientras algunos se encuentran físicamente insertos en zonas consolidadas, con mayor conectividad vial y cercanía a nodos comerciales, otros se desarrollan en condiciones de mayor aislamiento o fragmentación, producto de barreras físicas urbanas. Ésta heterogeneidad territorial genera grandes diferencias en el acceso efectivo a bienes y servicios básicos, incluyendo la oferta de alimentos, cuya disponibilidad, diversidad y precio pueden variar sensiblemente según el grado de integración urbana del barrio.

Desde esta perspectiva, resulta pertinente preguntarse en qué medida el costo de los insumos alimentarios en barrios populares está condicionado por factores como la accesibilidad física, la presencia de circuitos de comercialización formales o informales, el nivel de conectividad con zonas de mayor actividad económica, y las condiciones de seguridad o infraestructura urbana. Este trabajo buscará abordar dicha problemática a través del caso del Barrio Padre Carlos Mugica, combinando análisis espacial y recolección de datos cuantitativos para identificar patrones y correlaciones que permitan una mejor comprensión del fenómeno y orienten intervenciones de política pública en materia alimentaria y urbana.

La estrategia metodológica adoptada en este estudio combina un enfoque cuantitativo-descriptivo con herramientas de análisis territorial. En primer lugar, se delimita un recorte espacial que comprende el Barrio Padre Carlos Mugica y seis sectores próximos, seleccionados por su carácter predominantemente residencial y su cercanía geográfica. En cada uno de estos territorios se relevarán los precios de seis productos alimentarios representativos de la canasta básica, seleccionados por su consumo cotidiano y su peso en la dieta habitual de los hogares. A partir de estos datos se calculará un valor promedio de una “sub-canasta básica” alimentaria para el barrio Mugica y para cada uno de los sectores de comparación.

Este análisis permitirá evaluar las diferencias relativas de precios entre los distintos sectores y caracterizar la oferta alimentaria disponible para los residentes del barrio. A su vez, la información obtenida será contrastada con una serie de variables contextuales, tales como la densidad de locales comerciales, la densidad poblacional, los niveles de accesibilidad y conectividad vial, la irrigación de transporte público, indicadores de seguridad, y otros factores urbanos y socioeconómicos relevantes que permitan explorar posibles relaciones causales entre el entorno urbano y el costo de acceso a los alimentos.

Finalmente, a partir de los resultados obtenidos, se buscará esbozar propuestas de intervención —desde el ámbito público, privado o comunitario— orientadas a reducir las distorsiones o inequidades observadas en el mercado alimentario local, y contribuir así a mejorar las condiciones de vida de los habitantes del Barrio Mugica desde una perspectiva territorial.

<h2>
  LA UNIDAD TERRITORIAL </br>
  <small class="text-muted"> |  Recorte y selección </small>
</h2>

Tomo el polígono del barrio Padres Carlos Mugica y el de la ciudad de Buenos Aires.

Como el objetivo es observar el acceso a la ciudad que tiene el Barrio Mugica en comparación a la ciudad formal circundante, comienzo creando un buffer a 1500 metros del Barrio Mugica, y tomo los radios censales (censo 2022) que se intersectan con el mismo.

<details><summary> Código </summary> 
## Barrio 

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
#CARGO LAS BASES
ciudad = st_read("data/perimetro.geojson") %>% 
  #mutate(AREAKM2 = round((AREA/1000000),2)) %>% 
  st_transform(22185)%>% 
  st_make_valid()

b_mugica_sectores = st_read("data/sector.shp")

b_mugica <- st_buffer(st_union(b_mugica_sectores), 0.5) %>%
  st_sf() %>%  # convert to sf object with geometry column
  st_transform(22185) %>%
  mutate(area_km2 = as.numeric(st_area(geometry)) / 1e6)


#DEFINO 2 FUNCIONES PARA TRANSFORMAR LAS BASES ENTRE 4326 Y 22185 SEGUN SI TENGO QUE HACER ANÁLISIS GEOGRÁFICO O SI TENGO QUE MAPEAR:
transformar_todo_a_crs <- function(crs_destino = 4326) {
  objetos <- ls(envir = .GlobalEnv)

  for (nombre in objetos) {
    objeto <- get(nombre, envir = .GlobalEnv)
    if (inherits(objeto, "sf")) {
      nuevo <- st_transform(objeto, crs_destino)
      assign(nombre, nuevo, envir = .GlobalEnv)
    }
  }
}

```
## Buffer

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
#CREO UN BUFFER EN TORNO AL BARRIO PARA PODER SELECCIONAR LOS RADIOS CENSALES PRÓXIMOS:
b_buffer <- st_buffer(b_mugica, dist = 1500) %>% 
  st_transform(22185)

#LLAMO Y FILTRO LOS RADIOS CENSALES EN BASE A ESE BUFFER. LO HAGO UNA SOLA VEZ Y GUARDO PARA NO REPETIR NI CARGAR BASES MUY PESADAS:

#radios_censales = st_read("data/radios_censales.shp", options = "ENCODING=LATIN1")%>% 
#                    st_transform(22185) %>% 
#  filter(jur == "Ciudad Autónoma De Buenos Aires")

#st_write(radios_censales, "data/radios_censales_filtrados.geojson")

radios_censales = st_read("data/radios_censales_filtrados.geojson")%>% 
  st_transform(22185)

b_buffer = st_intersection(b_buffer,ciudad)

radios_filtrados = st_filter(radios_censales, b_buffer)

rm(list = c("radios_censales"), envir = .GlobalEnv)

```
</details>

Imagen 1. Ciudad Autónoma de Buenos Aires, polígono del Barrio Padre Carlos Mugica definido en la ley N° 6.129 (2018) y buffer de 1500m en torno al mismo.

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
transformar_todo_a_crs(4326)

mapa =
leaflet(st_zm(ciudad)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("CABA","Buffer Barrio Mugica","Barrio Mugica" ))%>% 
  
  addPolygons(data= ciudad,
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "CABA") %>% 

  addPolygons(data= b_buffer,
              color = "black",
              weight = 0.6,
              opacity = 0.7,
              fillColor = "#a64d79",
              fillOpacity = 0.3,
              popup = "<strong>BUFFER BARRIO - DISTANCIA:</strong>1500m",
              group = "Buffer Barrio Mugica") %>% 

  
  addPolygons(data= b_mugica,
              color = "black",
              weight = 0.8,
              opacity = 0.7,
              fillColor = "#a64d79",
              fillOpacity = 0.4,
              popup = ~paste("<strong>BARRIO P. MUGICA - AREA:</strong>", round(area_km2, 3), "km2"),
              group = "Barrio Mugica")
    
mapa

transformar_todo_a_crs(22185)
```

Imagen 2. Radios Censales que se intersectan con el buffer (imagen 2) 

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
transformar_todo_a_crs(4326)

mapa =
leaflet(st_zm(ciudad)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("Radios Censales","CABA","Barrio Mugica" ))%>% 
  
  addPolygons(data= ciudad,
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "CABA") %>% 
  
  addPolygons(data= b_mugica,
              color = "black",
              weight = 0.8,
              opacity = 0.7,
              fillColor = "#a64d79",
              fillOpacity = 0.4,
              popup = ~paste("<strong>AREA KM2:</strong>", round(area_km2, 3)),
              group = "Barrio Mugica") %>% 


  addPolygons(data= radios_filtrados,
              color = "black",
              weight = 0.6,
              opacity = 0.7,
              fillColor = "#a2c4c9",
              fillOpacity = 0.5,
              popup = ~paste("<strong> ID: </strong>", id),
              group = "Radios Censales") 

mapa

transformar_todo_a_crs(22185)
```

En base a eso, excluyo ciertos radios donde no predomina el uso residencial, ya que lo que busco observar es el acceso que tienen las personas desde sus viviendas a los servicios urbanos. Los resultantes serán los radios que comprenden el Barrio Mugica, y los sectores que cuentan con la proximidad suficiente como para considerarse el entorno urbano formal al que debe integrarse la urbanización informal.
Para filtrar los radios censales calculo la densidad de viviendas por radio censal y excluyo aquellos cuyo valor es menor a 3500.

El mapa resultante es el siguiente:

<details><summary> Código </summary>
## Radios Censales y Densidad de viviendas
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

#INCORPORO LA INFORMACION SOBRE VIVIENDAS DE LA BASE DE RADIOS CENSALES:
viviendas = read_excel("data/_tmp_11601061.xlsx", sheet = "data") %>% 
  rename("viviendas" = "C1", "cod_indec" = "Código") %>% 
  mutate(cod_indec = as.character(cod_indec),
         cod_indec = paste0("0",cod_indec))

#FILTRO POR DENSIDAD DE VIVIENDAS:
radios_viviendas = left_join(radios_filtrados, viviendas, by = "cod_indec") %>% 
  mutate(area_km2 = round(as.numeric(st_area(geometry)) / 1e6,4),
         densidad_viviendas = round(viviendas/area_km2,2)) %>% 
  filter(densidad_viviendas > 3500) %>%
  filter(!id %in% c("72473", "72474", "72419", "72418", "72557", "26526","50645","50665"))

#INCORPORO INFORMACION DE VIVIENDAS A LA BASE DEL BARRIO
radios_informal = st_filter(radios_viviendas, b_mugica) %>% 
  mutate(formal = "0") %>% 
  st_drop_geometry() %>% 
  select("cod_indec","formal")

#UNO AMBAS BASES Y DEFINO SI LOS RADIOS PERTENECEN A UN SECTOR FORMAL O INFORMAL
radios_selec = left_join(radios_viviendas, radios_informal, by = "cod_indec") %>% 
  mutate(formal = if_else(is.na(formal), "1", formal))

#ELIMINO BASES EN DESUSO
rm(list = c("radios_filtrados", "radios_viviendas", "viviendas", "b_buffer"), envir = .GlobalEnv)

```
</details>

Imagen 3. Radios censales pre-seleccionados.

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

radios_selec$formal = as.factor(radios_selec$formal)

pal_formal <- colorFactor(
  palette = c("0" = "#a64d79", "1" = "#a2c4c9"),
  domain = radios_selec$formal)

transformar_todo_a_crs(4326)

mapa =
leaflet(st_zm(ciudad)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("Radios Censales","CABA"))%>% 
  
  addPolygons(data= ciudad,
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "CABA") %>%

  addPolygons(data= radios_selec,
              color = "black",
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal_formal(formal),
              fillOpacity = 0.5,
              popup = ~paste("<strong> ID: </strong>", id,"<br><strong>DENSIDAD VIVIENDAS:</strong>", densidad_viviendas,"<br><strong>FORMAL:</strong>", formal),
              group = "Radios Censales") 

mapa

transformar_todo_a_crs(22185)
```

Calculo la sumatoria de los radios formales elegidos, y la divido entre el área del Barrio Mugica para agrupar los radios en "sectores próximos", y conformar así unidades comparables.

<details><summary> Código </summary>

## Definicion de sectores
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
#FILTRO LOS RADIOS CENSALES FORMALES
radios_formal = radios_selec %>% 
  filter(formal == "1")

#CALCULO EL AREA TOTAL DE LOS RADIOS FORMALES
area_formal = radios_formal %>% 
  summarise(area_km2=sum(area_km2))

#DIVIDO ESA AREA POR LA SUPERFICIE DEL BARRIO Y DEFINO DIVIDIRLO EN 6 SEGUN EL RESULTADO:
cantidad_sectores_prox = area_formal$area_km2/b_mugica$area_km2

#OBTENGO LOS CENTROIDES Y SUS COORDENADAS
centros = st_centroid(radios_formal) %>% 
  st_coordinates()

# APLICO K MEANS PARA AGRUPAR LOS RADIOS CENSALES EN 6 SECTORES SEGUN PROXIMIDAD
set.seed(123)  # Para reproducibilidad
kmeans_result = kmeans(centros, centers = 6)

# AGREGO EL ID DEL SECTOR A LA BASE
sectores_prox = radios_formal
sectores_prox$sector = as.numeric(kmeans_result$cluster) 

sectores_prox = sectores_prox%>% 
                filter(!grepl("3|6", sector)) %>% 
                mutate(sector = case_when(  #renombro los sectores por TOC, para que los numeros se vean en orden en el mapa...
                            sector == 2 ~ 7,
                            sector == 5 ~ 8,
                            sector == 4 ~ 9,
                            sector == 1 ~ 10,
                            .default = sector),
                       sector = case_when(
                            sector == 7 ~ 1,
                            sector == 8 ~ 2,
                            sector == 9 ~ 3,
                            sector == 10 ~ 4,
                            .default = sector))

radios_informal = radios_selec %>% 
  filter(formal == "0") %>% 
  mutate(sector = 0)

radios_tot = bind_rows(sectores_prox,radios_informal) %>% 
  select(-c(cde,dpto,jur,cpr,fid,cfn,cro,tro,sag,)) %>% 
  rename(id_radio = id,
         area_km2_r = area_km2,
         geometry_radio = geometry) %>% 
  arrange(sector)

sectores_tot = radios_tot %>% 
  group_by(sector) %>% 
  summarise(viviendas = sum(viviendas),
            area_km2_s = sum(area_km2_r),
            densidad_viviendas = viviendas/area_km2_s)%>% 
  arrange(sector)

rm(kmeans_result,area_formal,cantidad_sectores_prox, centros)
```
</details>

Los sectores próximos se agrupan entonces de la siguiente manera, y serán comparadas sus características con las del Barrio Mugica:

Imagen 5. Radios censales seleccionados agrupados en el Barrio Mugica y 4 sectores próximos.

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

pal = colorNumeric(
  palette = colorRampPalette(c("#a2c4c9", "#134f5c"))(100),
  domain = sectores_prox$sector
)

transformar_todo_a_crs(4326)

mapa =
leaflet(st_zm(sectores_prox)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("Sectores Proximos","Barrio Mugica"))%>% 
  
  addPolygons(data= radios_informal,
              color = "black",
              weight = 1,
              opacity = 0.3,
              fillColor = "#a64d79",
              fillOpacity = 0.5,
              popup = ~paste("<strong> ID: </strong>", id,"<br><strong>DENSIDAD VIVIENDAS:</strong>", densidad_viviendas,"<br><strong>FORMAL:</strong>", formal),
              group = "Barrio Mugica") %>%

  addPolygons(data= sectores_prox,
              color = "black",
              weight = 0.6,
              opacity = 0.3,
              fillColor = ~pal(sector),
              fillOpacity = 0.5,
              popup = ~paste("<strong> ID: </strong>", id,"<br><strong>DENSIDAD VIVIENDAS:</strong>", densidad_viviendas,"<br><strong>FORMAL:</strong>", formal,"<br><strong>sector:</strong>", sector),
              group = "Sectores Proximos") 

mapa

transformar_todo_a_crs(22185)
```

<h2>
  ACCESIBILIDAD </br>
  <small class="text-muted"> |  Áreas accesibles a pie </small>
</h2>

Para estudiar el acceso que tiene la población a los servicios de la ciudad, tengo en cuenta el traslado a pie y por medios de transporte públicos.
En primer lugar, trazo isocronas desde 5 puntos aleatorios definidos dentro del polígono del Barrio Mugica, y de cada uno de los sectores próximos.
Tomo los radios censales seleccionados y agrupados previamente y defino los polígonos totales de los sectores próximos.

<details><summary> Código </summary>
## Puntos Aleatorios e isocronas bm
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
#LLAMO A LOS SECTORES DEL BARRIO MUGICA PARA QUE LOS PUNTOS ALEATORIOS ESTÉN MÁS DISTRIBUIDOS. TODA ESTA PARTE LA HAGO UNA SOLA VEZ.

#GENERO Y GUARDO LOS PTOS ALEATORIOS PORQUE SINO CADA VEZ QUE CORRO EL CODIGO, SON NUEVOS:
#puntos_aleatorios_bm <- rowwise(b_mugica_sectores) %>%
#  mutate(punto_aleatorio = st_sample(geometry, 1, type = "random")) %>% 
#  st_drop_geometry(geometry) %>% 
#  st_as_sf()

#GUARDO LOS PUNTOS PARA LLAMARLOS DESPUES:
#st_write(puntos_aleatorios_bm, "data/puntos_aleatorios_bm.geojson")

#LLAMO A LOS PUNTOS ALEATORIOS GUARDADOS DEL BARRIO MUGICA:
puntos_aleatorios_bm = st_read("data/puntos_aleatorios_bm.geojson") %>% 
  st_transform(4326) %>% 
  clean_names() %>% 
  mutate(id_punto = paste0("0_",fid),
         sector = 0) %>% 
  select(-c(feat_id1,fid))

#CONSTRUYO ISOCRONAS DE 15M
#lista_isocronas_bm_15 <- lapply(puntos_aleatorios_bm$geometry, function(point) {
#  osrmIsochrone(loc = st_coordinates(point),
#                breaks = seq(from = 0, to = 15, by = 15),
#                res = 40,
#                osrm.profile = "foot")
#})

#Combino las isocronas
#isocronas15_bm = do.call(rbind, lista_isocronas_bm_15)

#st_write(isocronas15_bm, "data/isocronas15_bm.geojson")

#--------------------------

#CONSTRUYO ISOCRONAS DE 10M
#lista_isocronas_bm_10 <- lapply(puntos_aleatorios_bm$geometry, function(point) {
#  osrmIsochrone(loc = st_coordinates(point),
#                breaks = seq(from = 0, to = 10, by = 10),
#                res = 40,
#                osrm.profile = "foot")
#})

#Combino las isocronas
#isocronas10_bm = do.call(rbind, lista_isocronas_bm_10)

#st_write(isocronas10_bm, "data/isocronas10_bm.geojson")

#--------------------------

#CONSTRUYO ISOCRONAS DE 05M
#lista_isocronas_bm_05 <- lapply(puntos_aleatorios_bm$geometry, function(point) {
#  osrmIsochrone(loc = st_coordinates(point),
#                breaks = seq(from = 0, to = 5, by = 5),
#                res = 40,
#                osrm.profile = "foot")
#})

#Combino las isocronas
#isocronas05_bm = do.call(rbind, lista_isocronas_bm_05)

#st_write(isocronas05_bm, "data/isocronas05_bm.geojson")

#--------------------------------------------------------------------------------------

isocronas15_bm = st_read("data/isocronas15_bm.geojson")%>% 
                    st_transform(22185) %>% 
                    st_make_valid()%>% 
                    mutate(id_punto = paste0("0_",as.character(row_number())),
                           sector = 0) %>% 
                    select(sector,id_punto) 

isocronas10_bm = st_read("data/isocronas10_bm.geojson")%>% 
                    st_transform(22185) %>% 
                    st_make_valid()%>% 
                    mutate(id_punto = paste0("0_",as.character(row_number())),
                           sector = 0) %>%
                    select(sector,id_punto) 

isocronas05_bm = st_read("data/isocronas05_bm.geojson")%>% 
                    st_transform(22185) %>% 
                    st_make_valid()%>% 
                    mutate(id_punto = paste0("0_",as.character(row_number())),
                           sector = 0) %>%
                    select(sector,id_punto)
                    
```
## Puntos Aleatorios e isocronas sp
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
#ISOCRONAS SECTORES PROXIMOS
sectores_contorno = sectores_prox %>% 
  group_by(sector) %>% 
  summarise()

#GENERO Y GUARDO LOS PTOS ALEATORIOS PORQUE SINO CADA VEZ QUE CORRO EL CODIGO, SON NUEVOS:
#puntos_aleatorios_sp = sectores_contorno %>%
#  rowwise() %>%
#  mutate(puntos = list(st_sample(geometry, size = 9, type = "random"))) %>%
#  ungroup() %>%
#  select(-geometry) %>%  # elimina la geometría original
#  unnest(puntos) %>%
#  st_as_sf(coords = "puntos", crs = st_crs(sectores_contorno)) %>% 
#  st_drop_geometry() %>% 
#  rename("geometry"="puntos") %>% 
#  mutate(id_punto = paste0(sector,"_",row_number()))
  

#GUARDO LOS PUNTOS PARA LLAMARLOS DESPUES:
#st_write(puntos_aleatorios_sp, "data/puntos_aleatorios_sp.geojson")

#LLAMO A LOS PUNTOS ALEATORIOS GUARDADOS DE LOS SECTORES PROXIMOS:
puntos_aleatorios_sp = st_read("data/puntos_aleatorios_sp.geojson") %>% 
  st_transform(4326) %>% 
  clean_names() %>% 
  mutate(sector=as.numeric(sector))

#CONSTRUYO ISOCRONAS DE 15M
#lista_isocronas_sp_15_2 = lapply(seq_len(nrow(puntos_aleatorios_sp)), function(i) {
#  pt  <- puntos_aleatorios_sp[i, ]  # one-row sf (POINT)
#  iso <- osrmIsochrone(
#    loc = pt,                 # pass the sf row directly (no lon/lat cols needed)
#    breaks = c(0, 15),
#    res = 40,
#    osrm.profile = "foot"
#  )
#  mutate(iso, id_punto = pt$id_punto, sector = pt$sector)
#})

# Combine sf rows
#isocronas15_sp_2 = do.call(rbind, lista_isocronas_sp_15_2)

#st_write(isocronas15_sp_2, "data/isocronas15_sp.geojson")

#--------------------------------------------------------
#CONSTRUYO ISOCRONAS DE 10M
#lista_isocronas_sp_10_2 = lapply(seq_len(nrow(puntos_aleatorios_sp)), function(i) {
#  pt  <- puntos_aleatorios_sp[i, ]  # one-row sf (POINT)
#  iso <- osrmIsochrone(
#    loc = pt,                 # pass the sf row directly (no lon/lat cols needed)
#    breaks = seq(from = 0, to = 10, by = 10),
#    res = 40,
#    osrm.profile = "foot"
#  )
#  mutate(iso, id_punto = pt$id_punto, sector = pt$sector)
#})

# Combine sf rows
#isocronas10_sp_2 = do.call(rbind, lista_isocronas_sp_10_2)

#st_write(isocronas10_sp_2, "data/isocronas10_sp.geojson")

#--------------------------------------------------------
#CONSTRUYO ISOCRONAS DE 5M
#lista_isocronas_sp_05_2 = lapply(seq_len(nrow(puntos_aleatorios_sp)), function(i) {
#  pt  <- puntos_aleatorios_sp[i, ]  # one-row sf (POINT)
#  iso <- osrmIsochrone(
#    loc = pt,                 # pass the sf row directly (no lon/lat cols needed)
#    breaks = seq(from = 0, to = 5, by = 5),
#    res = 40,
#    osrm.profile = "foot"
#  )
#  mutate(iso, id_punto = pt$id_punto, sector = pt$sector)
#})

# Combine sf rows
#isocronas05_sp_2 = do.call(rbind, lista_isocronas_sp_05_2)

#st_write(isocronas05_sp_2, "data/isocronas05_sp.geojson")

#--------------------------------------------------------------------------------------

isocronas15_sp = st_read("data/isocronas15_sp.geojson")%>% 
                    st_transform(22185) %>% 
                    st_make_valid() %>% 
                    mutate(sector=as.numeric(sector))

isocronas15_tot = bind_rows(isocronas15_bm,isocronas15_sp)


#---------------------------------------------------------

isocronas10_sp = st_read("data/isocronas10_sp.geojson")%>% 
                    st_transform(22185) %>% 
                    st_make_valid() %>% 
                    mutate(sector=as.numeric(sector))

isocronas10_tot = bind_rows(isocronas10_bm,isocronas10_sp)

#---------------------------------------------------------

isocronas05_sp = st_read("data/isocronas05_sp.geojson")%>% 
                    st_transform(22185) %>% 
                    st_make_valid() %>% 
                    mutate(sector=as.numeric(sector))

isocronas05_tot = bind_rows(isocronas05_bm,isocronas05_sp)

```
  
</details>

Imagen 5. Polígonos globales de los sectores próximos conformados por sus radios censales.

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
transformar_todo_a_crs(4326)

ggplot(sectores_contorno) +
  geom_sf()

transformar_todo_a_crs(22185)
```

De esta manera, tengo 5 puntos aleatorios en cada sector, con una isocrona partiendo de cada uno. En total son 35 puntos e isocronas, contando los propios del Barrio.

Imagen 6. Isocronas de 15 minutos a pie desde los puntos aleatotios dentro del Barrio Mugica y de cada uno de los 6 sectores próximos.

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

transformar_todo_a_crs(4326)

mapa =
leaflet(st_zm(sectores_prox)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("Isocronas Barrio Mugica","Barrio Mugica", "Puntos Aleatorios BM", "Isocronas Sector 1", "Isocronas Sector 2", "Isocronas Sector 3", "Isocronas Sector 4", "Sectores Proximos", "Puntos Aleatorios SP"))%>% 
  
  addPolygons(data= isocronas15_bm,
              color = "#a64d79",
              weight = 1,
              opacity = 0.7,
              fillColor = "#a64d79",
              fillOpacity = 0.4,
              group = "Isocronas Barrio Mugica") %>%
  

  addPolygons(data= b_mugica,
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Barrio Mugica") %>%

  addPolygons(data= isocronas15_sp %>% filter(sector=="1"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 1") %>% 
  
    addPolygons(data= isocronas15_sp%>% filter(sector=="2"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 2") %>% 
  
    addPolygons(data= isocronas15_sp%>% filter(sector=="3"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 3") %>% 
  
    addPolygons(data= isocronas15_sp %>% filter(sector=="4"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 4") %>% 

  addPolygons(data= sectores_contorno,
              color = "black",
              weight = 0.6,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Sectores Proximos") %>% 
  
    addCircleMarkers(data=puntos_aleatorios_bm,
              fillColor = "black",
              weight = 0.5,
              color = "black",
              fillOpacity = 0.5,
              radius = 2,
              popup = ~paste("<strong> ID_punto: </strong>", id_punto),
              group = "Puntos Aleatorios BM") %>%

  addCircleMarkers(data=puntos_aleatorios_sp,
              fillColor = "black",
              weight = 0.5,
              color = "black",
              fillOpacity = 0.5,
              radius = 2,
              popup = ~paste("<strong> ID_punto: </strong>", id_punto),
              group = "Puntos Aleatorios SP")

mapa

transformar_todo_a_crs(22185)
```

<h2>
  ACCESIBILIDAD </br>
  <small class="text-muted"> |  Áreas accesibles en Transporte Público </small>
</h2>

Además de las zonas a las cuales la población puede acceder a pie, tengo en cuenta las que son accesible utilizando todos los medios de transporte público al alcance.
Para encontrar todos los medios de transporte a incluir, tomo las isocronas y las reduzco 150 metros en todo su contorno (equivalentes a 2 minutos a pie), ya que no tiene sentido utilizar aquellas paradas/estaciones/puntos de bicicletas de la ciudad que están en esa área, porque para ese momento, el viaje desde el punto ya habría durado 15 minutos, y es el límite superior que quiero conservar.
Además, debe tenerse en cuenta el tiempo promedio que lleva ir de una parada a otra, por lo que contar con menos implicaría no poder recorrer ni una parada desde el momento en el que se toma el colectivo por ejemplo.

El tiempo promedio que tarda un colectivo en recorrer una parada a otra en la Ciudad de Buenos Aires puede variar según diversos factores, como la línea específica, la hora del día y las condiciones del tráfico. Sin embargo, algunas estimaciones generales:

Velocidad promedio: En trayectos urbanos dentro de la ciudad, la velocidad promedio de los colectivos es de aproximadamente 10,5 km/h. Por ejemplo, un viaje entre Parque Centenario y Plaza de Mayo toma alrededor de 40 minutos en colectivo.

Distancia entre paradas: Las paradas suelen estar separadas por unos 400 metros en promedio, especialmente en corredores como el Metrobús Juan B. Justo.

Tiempo entre paradas: Dado lo anterior, el tiempo entre una parada y la siguiente suele oscilar entre 2 y 5 minutos, dependiendo del tráfico y la frecuencia del servicio.

Metrobús: En los corredores del Metrobús, que cuentan con carriles exclusivos, los tiempos de viaje pueden reducirse hasta en un 40%. 

Entonces, si tomo mínimo del tiempo estimado de una parada, 2 minutos, necesito reducir las isocronas a 13 minutos para seleccionar así los medios de transporte más alejados que se pueden alcanzar desde los puntos.
(Una persona promedio puede caminar entre 130 y 170 metros en 2 minutos en la ciudad, dependiendo del flujo peatonal, semáforos, y estado de las veredas.)



(https://www.infobae.com/tendencias/2016/12/12/como-se-viaja-en-transporte-publico-en-la-ciudad-de-buenos-aires/
https://www.clarin.com/ciudades/capital-gba-tarda-90-minutos-promedio-ir-volver-trabajo_0_Hkee2kFif.html?srsltid=AfmBOoovC3hiKDQGJMDZW-2lKp3s5LDfDqLUZcFnCsPbcLcvVbHwzUh6
https://buenosaires.gob.ar/sites/default/files/2023-04/Viajes_Transporte_P%C3%BAblico_202202%20con%20dise%C3%B1o%20actualizado.pdf
https://buenosaires.gob.ar/movilidad/metrobus/metrobus-juan-b-justo
https://www.buenosairesbus.com/seguimiento
https://transport.opendatasoft.com/explore/dataset/moovit-datos/table/
https://revistacolectibondi.com.ar/2024/12/12/cuanto-tiempo-pasamos-en-el-colectivo/
https://es.wikipedia.org/wiki/Metrob%C3%BAs_(Buenos_Aires))

Como OSMR(usado para calcular las isocronas) utiliza una medición de 83.33m/minuto, en vez de hacer nuevamente las isocronas a 13 minutos (proceso más costoso), las reduzco con un buffer a -166.66m.

<details><summary> Código </summary>
##Paradas Colectivos
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

#REDUZCO LAS ISOCRONAS EN 2 MINUTOS:
isocronas13_tot <- st_buffer(isocronas15_tot, dist = -166.66)%>% 
  st_transform(22185)

#INCORPORO LAS PARADAS DE COLECTIVOS (LO HAGO UNA VEZ Y DESPUÉS LLAMO DIRECTAMENTE LAS FILTRADAS)

#colectivos_paradas = st_read("https://cdn.buenosaires.gob.ar/datosabiertos/datasets/transporte-y-obras-publicas/colectivos-paradas/paradas-de-colectivo.geojson") %>% 
#  st_transform(22185)

#paradas_tot = st_join(colectivos_paradas, isocronas13_tot, join = st_within, left = FALSE)%>%
#  select(where(~ !all(is.na(.)))) %>% 
#  clean_names() %>% 
#  select(-c(calle,alt_plano,coord_x,coord_y,comuna,barrio,fid)) %>% 
#  mutate(id_punto=id_punto) %>% 
#  rename(geometry_parada = geometry)

#st_write(paradas_tot, "data/paradas_tot.geojson")

paradas_tot = st_read("data/paradas_tot.geojson")%>% 
  rename(geometry_parada=geometry) %>% 
  st_transform(22185) 


```
</details>

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

transformar_todo_a_crs(4326)

mapa =
leaflet(st_zm(sectores_prox)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("Isocronas Barrio Mugica","Barrio Mugica", "Puntos Aleatorios BM", "Isocronas Sector 1", "Isocronas Sector 2", "Isocronas Sector 3", "Isocronas Sector 4", "Isocronas Sector 5", "Isocronas Sector 6", "Sectores Proximos", "Puntos Aleatorios SP", "Paradas SP", "Paradas BM"))%>% 
  
  addPolygons(data= isocronas15_bm,
              color = "#a64d79",
              weight = 1,
              opacity = 0.7,
              fillColor = "#a64d79",
              fillOpacity = 0.4,
              group = "Isocronas Barrio Mugica") %>%
  

  addPolygons(data= b_mugica,
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Barrio Mugica") %>%

  addPolygons(data= isocronas15_sp %>% filter(sector=="1"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 1") %>% 
  
    addPolygons(data= isocronas15_sp%>% filter(sector=="2"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 2") %>% 
  
    addPolygons(data= isocronas15_sp%>% filter(sector=="3"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 3") %>% 
  
    addPolygons(data= isocronas15_sp %>% filter(sector=="4"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 4") %>% 
  
    addPolygons(data= isocronas15_sp %>% filter(sector=="5"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 5") %>% 
  
    addPolygons(data= isocronas15_sp %>% filter(sector=="6"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 6") %>% 

  addPolygons(data= sectores_contorno,
              color = "black",
              weight = 0.6,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Sectores Proximos") %>% 
  
    addCircleMarkers(data=puntos_aleatorios_bm,
              fillColor = "black",
              weight = 0.5,
              color = "black",
              fillOpacity = 0.5,
              radius = 2,
              group = "Puntos Aleatorios BM") %>%

  addCircleMarkers(data=puntos_aleatorios_sp,
              fillColor = "black",
              weight = 0.5,
              color = "black",
              fillOpacity = 0.5,
              radius = 2,
              group = "Puntos Aleatorios SP") %>% 
  
  addCircleMarkers(data=paradas_tot %>% filter(sector!=0),
              fillColor = "#ffd966",
              weight = 0.5,
              color = "#ffd966",
              fillOpacity = 1,
              radius = 1,
              group = "Paradas SP") %>% 
  
    addCircleMarkers(data=paradas_tot %>% filter(sector==0),
              fillColor = "#ffd966",
              weight = 0.5,
              color = "#ffd966",
              fillOpacity = 1,
              radius = 1,
              group = "Paradas BM")

mapa

transformar_todo_a_crs(22185)
```


</details>

<h2>
  LOCALES COMERCIALES </br>
  <small class="text-muted"> |  Dentro de los polígonos </small>
</h2>

Comercios dentro del Barrio Mugica

<details><summary> Código </summary>
## Comercios BM
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

#VEO QUE CAPAS TENGO EN INFORMACION TERRITORIAL, NO LLAMAR TODO.
#print(st_layers("data/informacion_territorial.gpkg"))

#ME TRAIGO LA CAPA DE COMERCIOS Y LA FILTRO SEGÚN LOS QUE CORRESPONDEN A LA INVESTIGACIÓN.
comercios1_bm = st_read("data/informacion_territorial.gpkg", layer = "comercios") %>% 
  filter(Tipo == "Comercio",
         grepl("ALMACÉN|Kiosko|Carnicer|Fiambrería|Verdulería", Rubro),
         !st_is_empty(geom)) %>% 
  st_transform(22185) %>% 
  mutate(Altura = as.numeric(Altura)) %>% 
  rename(geometry = geom) %>% 
  arrange(Calle, Altura) %>% 
  clean_names() %>% 
  select(direccion)


#GUARDO LAS OBSERVACIONES PARA IR A RELEVAR AL TERRITORIO INCORPORANDO LA INFORMACIÓN Y ACTUALIZANDO LOS DATOS YA QUE SON DEL 2022
#st_write(comercios1_bm,"data/comercios1_bm.xlsx")
comercios_sin_geom = read_excel("data/comercios_bm_sin_geom.xlsx") %>%
  clean_names()

comercios_sin_geom$geometry <- st_as_sfc(comercios_sin_geom$geometry)
 
comercios_sin_geom = comercios_sin_geom %>% 
  st_as_sf(crs = 4326) %>% 
  st_transform(22185)

comercios2_bm = rbind(comercios1_bm, comercios_sin_geom)

comercios_bm_territorio = read_excel("data/comercios_bm_territorio.xlsx") %>%
  clean_names()

comercios_bm_cadena = comercios_bm_territorio %>% 
  filter(cadena == "si") 

comercios_bm_cadena$geometry <- st_as_sfc(comercios_bm_cadena$geometry)

comercios_bm_cadena = comercios_bm_cadena %>% 
                      st_as_sf(crs = 4326) %>% 
                      st_transform(22185)


comercios_bm_local = comercios_bm_territorio %>% 
  filter(cadena == "no") %>% 
  select(-geometry)
  

comercios_bm_local_geom = left_join(comercios_bm_local, comercios2_bm, by = "direccion") %>% 
  mutate(relevado = as.factor(if_else(is.na(leche), 0, 1))) %>% 
  st_as_sf() %>% 
  st_transform(22185)


comercios_bm = rbind(comercios_bm_local_geom, comercios_bm_cadena) %>% 
  filter(!st_is_empty(geometry)) %>% 
  mutate(id_comercio = paste0("cbm_", str_pad(01:(00 + n()), width = 4, pad = "0")))


rm(comercios1_bm, comercios_sin_geom, comercios2_bm, comercios_bm_cadena, comercios_bm_local, comercios_bm_territorio, comercios_bm_local_geom )
```
</details>


Comercios en los sectores formales próximos

<details><summary> Código inútil-arreglar </summary>
## Comercios SP
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
#IMPORTO LOS COMERCIOS MAPEADOS DE CABA Y LOS FILTRO PARA QUEDARME CON LOS QUE CAEN EN LAS ISOCRONAS DE LOS SP
comercios_sp1 = st_read("data/meu_tesis_supermercados_caba.kml") %>% 
  st_make_valid() %>% 
  st_zm() %>% 
  st_transform(22185) %>% 
  st_filter(isocronas15_sp) %>% 
  clean_names() %>% 
  select(-description) %>% 
  rename(rubro = name) %>% 
  mutate(cadena = "si",
         id_comercio = paste0("csp_", str_pad(76:(75 + n()), width = 4, pad = "0")))


comercios_sp2 = read_excel("data/comercios_territorio_sp.xlsx") %>%
  clean_names()

comercios_sp2$geometry <- st_as_sfc(comercios_sp2$geometry)

comercios_sp2 = comercios_sp2 %>% 
                st_as_sf(crs = 4326) %>% 
                st_transform(22185)

comercios_sp = bind_rows(comercios_sp2,comercios_sp1)

comercios_tot = bind_rows(comercios_sp, comercios_bm)

rm(comercios_sp1,comercios_sp2)
```
</details>

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

transformar_todo_a_crs(4326)

mapa =
leaflet(st_zm(sectores_prox)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("Barrio Mugica", "Isocronas Sector 1", "Isocronas Sector 2", "Isocronas Sector 3", "Isocronas Sector 4", "Sectores Proximos", "Puntos Aleatorios SP", "Comercios Tot"))%>% 
  


  addPolygons(data= b_mugica,
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Barrio Mugica") %>%

  addPolygons(data= isocronas15_sp %>% filter(sector=="1"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 1") %>% 
  
    addPolygons(data= isocronas15_sp%>% filter(sector=="2"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 2") %>% 
  
    addPolygons(data= isocronas15_sp%>% filter(sector=="3"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 3") %>% 
  
    addPolygons(data= isocronas15_sp %>% filter(sector=="4"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 4") %>%  

  addPolygons(data= sectores_contorno,
              color = "black",
              weight = 0.6,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Sectores Proximos") %>% 
  

  addCircleMarkers(data=puntos_aleatorios_sp,
              fillColor = "black",
              weight = 0.5,
              color = "black",
              fillOpacity = 0.5,
              radius = 2,
              group = "Puntos Aleatorios SP") %>% 
  

  addCircleMarkers(data = comercios_tot,
              fillColor = "#f6b26b",
              weight = 0.5,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              group = "Comercios Tot")

mapa

transformar_todo_a_crs(22185)
```


Comercios por isocrona

<details><summary> Código </summary>
## Comercios por isocrona
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
#CRUZO LOS COMERCIOS CON LAS ISOCRONAS
comercios_por_isocrona = st_join(comercios_tot, isocronas15_tot, join = st_intersects, left = TRUE)

#CALCULO LA CANTIDAD DE COMERCIOS POR ISOCRONA
n_comercios_x_iso = comercios_por_isocrona %>% 
  group_by(id_punto) %>% 
  summarise(n_comercios = n())

vars = c("leche","harina","azucar","arroz","papa","fiambre_jc", "carne_pic")

# ORGANIZO LA INFORMACION POR ISOCRONA, COMERCIO, PRODUCTO (LARGO)
df_long <- comercios_por_isocrona %>%
  select(id_punto, id_comercio, all_of(vars)) %>%
  pivot_longer(cols = all_of(vars), names_to = "product", values_to = "price") %>%
  filter(!is.na(price) & price > 0)

# CANASTA MAS ECONOMICA Y MÁS COSTOSA POR ISOCRONA
# USO (p5 y p95) DE BASE EN LUGAR DE LOS VALORES MINIMOS Y MAXIMOS PARA EVITAR ERRORES/OUTLIERS
iso_prod_limites <- df_long %>%
  group_by(id_punto, product) %>%
  summarise(
    p10 = quantile(price, 0.05, na.rm = TRUE),
    pup = quantile(price, 0.95, na.rm = TRUE),
    n   = sum(is.finite(price)),
    .groups = "drop"
  ) %>%
  # USO SOLO LOS PRODUCTOS CON AMBOS CUANTILES DEFINIDOS
  filter(is.finite(p10), is.finite(pup))

# Canastas por isocrona (suma y geométrica) y "margen" (brecha min–max)
iso_canasta <- iso_prod_limites %>%
  group_by(id_punto) %>%
  summarise(
    # Canasta mínima (usa p10)
    canasta_sum_min = sum(p10, na.rm = TRUE),
    canasta_gm_min  = exp(mean(log(p10), na.rm = TRUE)),

    # Canasta "más costosa" (usa p90/p95)
    canasta_sum_max = sum(pup, na.rm = TRUE),
    canasta_gm_max  = exp(mean(log(pup), na.rm = TRUE)),

    n_products_both = n(),  # nº de productos usados en ambas
    .groups = "drop"
  ) %>%
  mutate(
    # Brecha absoluta en suma y % en geométrica
    margen_sum     = canasta_sum_max - canasta_sum_min,
    margen_gm_pct  = canasta_gm_max / canasta_gm_min - 1,
    # equivalente en logs (útil para tests y promedios)
    margen_log     = log(canasta_gm_max) - log(canasta_gm_min),
    formal = ifelse(str_starts(id_punto,"0"), 0, 1)
  )
```


```{r}
sumtab <- iso_canasta %>%
  group_by(formal) %>%
  summarise(
    n_iso          = n(),
    # GM “cara”
    mean_gm_max    = mean(canasta_gm_max, na.rm = TRUE),
    median_gm_max  = median(canasta_gm_max, na.rm = TRUE),
    wmean_gm_max   = weighted.mean(canasta_gm_max, n_products_both, na.rm = TRUE),
    # Brecha (log)
    mean_head_log  = mean(margen_log, na.rm = TRUE),
    median_headpct = median(margen_gm_pct, na.rm = TRUE),
    .groups = "drop"
  )
print(sumtab)
```


```{r}
# Asegura que el grupo sea factor con niveles y etiquetas claras
iso_canasta <- iso_canasta %>%
  mutate(grupo = factor(ifelse(formal == 1, "formal", "informal"),
                        levels = c("formal","informal")))

# t-tests
t_gmmax <- t.test(log(canasta_gm_max) ~ grupo, data = iso_canasta)   # WTP (cola alta)
t_head  <- t.test(margen_log       ~ grupo, data = iso_canasta)    # brecha interna

# Convierte diferencia en logs -> % y orienta a (slum - formal)
conv_pct <- function(tt){
  nm <- tolower(names(tt$estimate))
  m_formal   <- unname(tt$estimate[nm == "mean in group formal"])
  m_informal <- unname(tt$estimate[nm == "mean in group informal"])
  diff_log <- m_informal - m_formal                 # informal − formal
  ci_log   <- sort(-tt$conf.int)                    # pasa a (informal − formal)
  c(
    pct_diff = 100*(exp(diff_log) - 1),
    ci_low   = 100*(exp(ci_log[1]) - 1),
    ci_high  = 100*(exp(ci_log[2]) - 1),
    p_value  = unname(tt$p.value)
  )
}

res_gmmax <- conv_pct(t_gmmax)   # % dif en GM de canasta “cara” (slum − formal)
res_head  <- conv_pct(t_head)    # % dif en margen (slum − formal)

res_gmmax
res_head

# Robustez no paramétrica
wil_gmmax <- wilcox.test(log(canasta_gm_max) ~ grupo, data = iso_canasta, exact = FALSE)
wil_head  <- wilcox.test(margen_log       ~ grupo, data = iso_canasta, exact = FALSE)
wil_gmmax$p.value; wil_head$p.value
```
</details>

La canasta de precios altos (p90) es 31,5% menor en isócronas informales (IC95%: −33,5% a −29,4%). Asimismo, el margen interno es 42,4% menor (IC95%: −45,2% a −39,5%), lo que sugiere menor variedad/estratificación y una menor disposición a pagar en el extremo alto respecto de las áreas formales.

<details><summary> Código </summary>

```{r}
#--- 1) Cuantiles por isocrona × producto (p10 como "piso") -------------------
iso_prod_floor <- df_long %>%
  filter(is.finite(price), price > 0) %>%
  group_by(id_punto, product) %>%
  summarise(p10 = quantile(price, 0.10, na.rm = TRUE),
            n  = dplyr::n(), .groups = "drop") %>%
  filter(is.finite(p10))

#--- 2) Etiqueta de grupo por isocrona ----------------------------------------
# Opción A: tomarla de tu tabla (recomendado)
iso_tags <- iso_canasta %>%
  distinct(id_punto, grupo)   # grupo con niveles: "formal","informal"

# Opción B (si no tenés iso_canasta a mano): derivar desde el id
# iso_tags <- iso_prod_floor %>%
#   distinct(id_punto) %>%
#   mutate(grupo = if_else(str_starts(id_punto, "0"), "informal", "formal"))

iso_prod2 <- iso_prod_floor %>%
  inner_join(iso_tags, by = "id_punto") %>%
  mutate(grupo = factor(grupo, levels = c("formal","informal"))) %>%
  group_by(product) %>%
  filter(n_distinct(grupo) == 2) %>%   # nos quedamos con productos presentes en ambos grupos
  ungroup()

#--- 3) Efecto por producto (Welch t-test en log precios) ----------------------
per_product <- iso_prod2 %>%
  group_by(product) %>%
  tidyr::nest() %>%
  mutate(
    tt = map(data, ~ t.test(log(p10) ~ grupo, data = .x)),                     # formal vs informal
    # diferencia en logs orientada a (informal − formal)
    diff_log = map_dbl(tt, ~ {
      est <- .x$estimate; nm <- tolower(names(est))
      m_formal   <- unname(est[nm == "mean in group formal"])
      m_informal <- unname(est[nm == "mean in group informal"])
      m_informal - m_formal
    }),
    ci_log = map(tt, ~ sort(-.x$conf.int)),                                    # CI en (informal − formal)
    pct     = 100*(exp(diff_log) - 1),
    pct_lo  = map_dbl(ci_log, 1) %>% (\(.) 100*(exp(.) - 1))(),
    pct_hi  = map_dbl(ci_log, 2) %>% (\(.) 100*(exp(.) - 1))(),
    p_value = map_dbl(tt, "p.value"),
    n_formal   = map_int(data, ~ sum(.x$grupo == "formal")),
    n_informal = map_int(data, ~ sum(.x$grupo == "informal"))
  ) %>%
  select(product, pct, pct_lo, pct_hi, p_value, n_formal, n_informal) %>%
  arrange(desc(pct))

per_product

#--- 4) Gráfico (igual estilo que el tuyo) -------------------------------------
plot = ggplot(per_product, aes(x = reorder(product, pct), y = pct)) +
  geom_pointrange(aes(ymin = pct_lo, ymax = pct_hi)) +
  geom_hline(yintercept = 0, linetype = 2) +
  coord_flip() +
  labs(
    x = NULL,
    y = "Prima informal vs formal (%)",
    title = "Per-product least-cost floor price (p10) — informal vs formal",
    subtitle = "Puntos = diferencia porcentual; barras = IC95% (Welch t-test en log precios)"
  ) +
  theme_minimal()


```
</details>

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
plot
```

----------------------
Las diferencias de precios observadas son consistentes con una menor ingesta de carne y una mayor sustitución hacia almidones en áreas informales. En efecto, la carne presenta un sobreprecio marcado en el p10, mientras que papa resulta relativamente más barata.

SUSTENTAR CON ENCUESTAS A HOGARES?
---------------------
<details><summary> Código </summary>
```{r}
min_obs = 3

# 1) Cuantiles por isócrona × producto
iso_prod <- df_long %>%
  filter(is.finite(price), price > 0) %>%
  group_by(id_punto, product) %>%
  summarise(
    p10 = quantile(price, 0.10, na.rm = TRUE),
    pup = quantile(price, 0.90, na.rm = TRUE),
    n   = n(),
    .groups = "drop"
  ) %>%
  filter(n >= min_obs, is.finite(p10), is.finite(pup))

# 2) Etiqueta de grupo y filtros globales
iso_tags <- iso_canasta %>% distinct(id_punto, grupo)  # grupo: "formal"/"informal"

iso_prod2 <- iso_prod %>%
  filter(!str_detect(id_punto, "^(3|6)_")) %>%          # tus exclusiones
  inner_join(iso_tags, by = "id_punto") %>%
  mutate(grupo = factor(grupo, levels = c("formal","informal"))) %>%
  # headroom dentro de isócrona × producto
  mutate(headroom_log = log(pup) - log(p10),
         headroom_pct = exp(headroom_log) - 1)

# (opcional) exige cobertura mínima por grupo para cada producto
iso_prod2 <- iso_prod2 %>%
  group_by(product, grupo) %>% filter(n() >= 3) %>% ungroup()

# 3) Diferencia de headroom por producto: (informal − formal) en %
per_prod_head <- iso_prod2 %>%
  group_by(product) %>%
  tidyr::nest() %>%
  mutate(
    tt = map(data, ~ t.test(headroom_log ~ grupo, data = .x)),   # Welch
    diff_log = map_dbl(tt, ~ {
      est <- .x$estimate; nm <- tolower(names(est))
      m_formal   <- unname(est[nm == "mean in group formal"])
      m_informal <- unname(est[nm == "mean in group informal"])
      m_informal - m_formal                                    # informal − formal
    }),
    ci_log = map(tt, ~ sort(-.x$conf.int)),                     # pasa a (informal − formal)
    pct    = 100*(exp(diff_log) - 1),
    pct_lo = map_dbl(ci_log, 1) %>% (\(.) 100*(exp(.) - 1))(),
    pct_hi = map_dbl(ci_log, 2) %>% (\(.) 100*(exp(.) - 1))(),
    p_value = map_dbl(tt, "p.value"),
    n_formal   = map_int(data, ~ sum(.x$grupo == "formal")),
    n_informal = map_int(data, ~ sum(.x$grupo == "informal"))
  ) %>%
  select(product, pct, pct_lo, pct_hi, p_value, n_formal, n_informal) %>%
  arrange(desc(pct))

per_prod_head

# 4) Gráfico: diferencia de headroom (%) por producto (informal − formal)
margen_plot = ggplot(per_prod_head, aes(x = reorder(product, pct), y = pct)) +
  geom_pointrange(aes(ymin = pct_lo, ymax = pct_hi)) +
  geom_hline(yintercept = 0, linetype = 2) +
  coord_flip() +
  labs(
    x = NULL,
    y = "Diferencia de headroom (%) — informal − formal",
    title = "Headroom por producto (p90 vs p10)",
    subtitle = "Puntos = diferencia porcentual; barras = IC95% (Welch t-test sobre log-ratios)"
  ) +
  theme_minimal()
```
</details>

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
margen_plot
```

<details><summary> Código </summary>
```{r}
iso_canasta2 <- iso_canasta %>%
  mutate(
    grupo = factor(ifelse(formal == 1, "formal", "informal"),
                   levels = c("formal","informal")),
    carga_log   = log(canasta_gm_min) - log(canasta_gm_max),  # = -headroom_log
    carga_share = exp(carga_log)                              # = GM_min / GM_max
  )

# Descriptivos
iso_canasta2 %>%
  group_by(grupo) %>%
  summarise(
    mean_share   = mean(carga_share, na.rm = TRUE),
    median_share = median(carga_share, na.rm = TRUE),
    .groups = "drop"
  )

# Test en logs (interpretación % al exponenciar)
t_carga <- t.test(carga_log ~ grupo, data = iso_canasta2)  # informal vs formal en logs

# Razón de “carga relativa” (informal/formal) y su IC95% en %
rat   <- exp(unname(t_carga$estimate["mean in group informal"] -
                    t_carga$estimate["mean in group formal"]))
ci    <- exp(sort(-t_carga$conf.int))  # pasa a (informal − formal)
pct   <- 100*(rat - 1)
ci_lo <- 100*(ci[1] - 1); ci_hi <- 100*(ci[2] - 1)

c(rel_diff_pct = pct, ci_low = ci_lo, ci_high = ci_hi, p_value = t_carga$p.value)
```
</details>

En nuestra muestra, el costo relativo del piso de precios (GM_min/GM_max) es significativamente mayor en isócronas informales, aun cuando algunos precios absolutos sean menores. Esto implica que los bienes básicos representan una mayor fracción del ‘techo’ de precios local (proxy de poder adquisitivo/WTP) en zonas informales.

El índice de carga relativa carga=GM_min/GM_max es 73.6% mayor en las isócronas informales (IC95%: 65.3%–82.3%, p ≈ 1.0e−17).

Es decir que, dado el “techo” local de precios (proxy de WTP), el costo de la canasta barata representa una fracción mucho mayor en áreas informales.

<details><summary> Código </summary>
```{r}
iso_prod <- df_long %>%
  filter(is.finite(price), price > 0) %>%
  group_by(id_punto, product) %>%
  summarise(
    p10 = quantile(price, 0.10, na.rm = TRUE),
    p_up = quantile(price, 0.90, na.rm = TRUE),
    n   = n(),
    .groups = "drop"
  ) %>%
  filter(n >= min_obs, is.finite(p10), is.finite(p_up))

# etiqueta de grupo por isócrona
iso_tags <- iso_canasta %>% distinct(id_punto, grupo)   # "formal"/"informal"

iso_prod2 <- iso_prod %>%
  # excluye isócronas con poca info si corresponde
  filter(!str_detect(id_punto, "^(3|6)_")) %>%
  inner_join(iso_tags, by = "id_punto") %>%
  mutate(
    grupo = factor(grupo, levels = c("formal","informal")),
    carga_log = log(p10) - log(p_up),         # log(p10/p90) (o p95)
    carga_pct = exp(carga_log) - 1
  )

# (opcional) excluir productos dominantes como carne
# iso_prod2 <- filter(iso_prod2, product != "carne_pic")

# exige cobertura mínima por grupo (evita comparar con 1–2 isócronas)
iso_prod2 <- iso_prod2 %>%
  group_by(product, grupo) %>% filter(n() >= 3) %>% ungroup()

#--------------------------
# 2) Diferencia por producto: (informal − formal) en %
#    Welch t-test sobre logs (equivale a % al exponenciar)
#--------------------------
per_prod_carga <- iso_prod2 %>%
  group_by(product) %>%
  nest() %>%
  mutate(
    tt = map(data, ~ t.test(carga_log ~ grupo, data = .x)),
    diff_log = map_dbl(tt, ~ {
      est <- .x$estimate; nm <- tolower(names(est))
      m_formal   <- unname(est[nm == "mean in group formal"])
      m_informal <- unname(est[nm == "mean in group informal"])
      m_informal - m_formal                      # informal − formal
    }),
    ci_log = map(tt, ~ sort(-.x$conf.int)),      # pasa CI a (informal − formal)
    pct    = 100*(exp(diff_log) - 1),
    pct_lo = map_dbl(ci_log, 1) %>% (\(.) 100*(exp(.) - 1))(),
    pct_hi = map_dbl(ci_log, 2) %>% (\(.) 100*(exp(.) - 1))(),
    p_value = map_dbl(tt, "p.value"),
    n_formal   = map_int(data, ~ sum(.x$grupo == "formal")),
    n_informal = map_int(data, ~ sum(.x$grupo == "informal"))
  ) %>%
  select(product, pct, pct_lo, pct_hi, p_value, n_formal, n_informal) %>%
  arrange(desc(pct))

per_prod_carga

carga_plot = ggplot(per_prod_carga, aes(x = reorder(product, pct), y = pct)) +
  geom_pointrange(aes(ymin = pct_lo, ymax = pct_hi)) +
  geom_hline(yintercept = 0, linetype = 2) +
  coord_flip() +
  labs(
    x = NULL,
    y = "Diferencia de carga (%) — informal − formal",
    title = "carga por producto (p10/p90)",
    subtitle = "Puntos = diferencia porcentual; barras = IC95% (Welch t-test sobre log-ratios)"
  ) +
  theme_minimal()
```
</details>

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
carga_plot
```


<h2>
  IRRIGACIÓN </br>
  <small class="text-muted"> |  Calles comprendidas en los sectores y accesos </small>
</h2>

Intersectando el callejero con los polígonos del Barrio Mugica y de los sectores próximos puedo calcular la irrigación vial al polígono.

<details><summary> Código </summary>
## Callejero
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
#IMPORTO EL CALLEJERO:
callejero = st_read("data/calles.shp") %>% 
  st_transform(22185)


#GENERO PUNTOS EN LAS INTERSECCIONES DE LAS CALLES CON LOS POLIGONOS DE LOS SECTORES PARA CALCULAR ACCESIBILIDAD

# This gives you actual POINT geometries where the lines intersect polygon boundaries
interseccion_callejero_bm = st_intersection(
  st_cast(callejero, "LINESTRING"),
  st_boundary(b_mugica) %>% 
    st_transform(22185)) %>%
  filter(st_geometry_type(.) == "POINT")  # ensures only POINT geometries

interseccion_callejero_sp <- st_intersection(
  st_cast(callejero, "LINESTRING"),
  st_boundary(sectores_contorno) %>% 
    st_transform(22185)) %>%
  filter(st_geometry_type(.) == "POINT")  # ensures only POINT geometries


# Then count per line if needed
n_interseccion_callejero_bm = interseccion_callejero_bm %>%
  st_drop_geometry() %>%
  count()

n_interseccion_callejero_sp <- interseccion_callejero_sp %>%
  st_drop_geometry() %>%
  group_by(sector) %>%
  summarise(n_intersecciones = n())


```
</details>


```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

transformar_todo_a_crs(4326)

mapa =
leaflet(st_zm(sectores_prox)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("Barrio Mugica","Accesos BM", "Sector Próximo 1", "Accesos SP 1", "Sector Próximo 2", "Accesos SP 2", "Sector Próximo 3", "Accesos SP 3", "Sector Próximo 4", "Accesos SP 4", "Sector Próximo 5", "Accesos SP 5", "Sector Próximo 6", "Accesos SP 6"))%>% 
  


  addPolygons(data= b_mugica,
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Barrio Mugica") %>%
  
   addCircleMarkers(data = interseccion_callejero_bm,
              fillColor = "#f6b26b",
              weight = 0.5,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              group = "Accesos BM") %>% 
  

      addPolygons(data= sectores_contorno %>% filter(sector=="1"),
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Sector Próximo 1") %>% 
  

  addCircleMarkers(data = interseccion_callejero_sp%>% filter(sector=="1"),
              fillColor = "#f6b26b",
              weight = 0.5,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              group = "Accesos SP 1") %>% 
  
  
  addPolygons(data= sectores_contorno %>% filter(sector=="2"),
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Sector Próximo 2") %>% 
  

  addCircleMarkers(data = interseccion_callejero_sp%>% filter(sector=="2"),
              fillColor = "#f6b26b",
              weight = 0.5,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              group = "Accesos SP 2") %>% 
  
  addPolygons(data= sectores_contorno %>% filter(sector=="3"),
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Sector Próximo 3") %>% 
  

  addCircleMarkers(data = interseccion_callejero_sp%>% filter(sector=="3"),
              fillColor = "#f6b26b",
              weight = 0.5,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              group = "Accesos SP 3") %>% 
  
  addPolygons(data= sectores_contorno %>% filter(sector=="4"),
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Sector Próximo 4") %>% 
  

  addCircleMarkers(data = interseccion_callejero_sp%>% filter(sector=="4"),
              fillColor = "#f6b26b",
              weight = 0.5,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              group = "Accesos SP 4") %>% 
  
  addPolygons(data= sectores_contorno %>% filter(sector=="5"),
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Sector Próximo 5") %>% 
  

  addCircleMarkers(data = interseccion_callejero_sp%>% filter(sector=="5"),
              fillColor = "#f6b26b",
              weight = 0.5,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              group = "Accesos SP 5") %>% 
  
  addPolygons(data= sectores_contorno %>% filter(sector=="6"),
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Sector Próximo 6") %>% 
  

  addCircleMarkers(data = interseccion_callejero_sp%>% filter(sector=="6"),
              fillColor = "#f6b26b",
              weight = 0.5,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              group = "Accesos SP 6")

mapa

transformar_todo_a_crs(22185)
```


<details><summary> Código </summary>
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
n_interseccion_callejero_bm = n_interseccion_callejero_bm %>% 
  mutate(sector = "Barrio Mugica 0") %>% 
  rename(n_intersecciones = n)

n_interseccion_callejero_sp = n_interseccion_callejero_sp %>% 
  mutate(sector = paste0("Sector Proximo ", sector))

intersecciones_tabla = bind_rows(n_interseccion_callejero_sp, n_interseccion_callejero_bm) 
```
</details>

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
tabla =
datatable(intersecciones_tabla%>% 
            select(sector, n_intersecciones), 
  style = 'bootstrap', 
  class = 'cell-border stripe',
  options = list(
    searching = FALSE,
    rownames = FALSE,
    pageLength = 10,
    dom = 't',
    scrollY = '300px',
    language = list(zeroRecords = "No existen datos para los requerimientos seleccionados"),
    columnDefs = list(
      list(targets = 0, visible = FALSE), # hide column 0
      list(targets = c(1,2), width = '150px', className = 'dt-left'))
    )
  )

tabla

```



Avenida:
Calee:
Pasaje:
Pasillo: Ancho variable  de 2 m a 1,35 m.  No es calle oficial por ley 6129. 

<details><summary> Código </summary>
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

#FILTRO LOS QUE SON ALMACENES:
callejero_bm = st_read("data/informacion_territorial.gpkg", layer = "calles") %>% 
  st_transform(22185) %>% 
  filter(!is.na(NOMBRE)) %>% 
  mutate(tipo_c = ifelse(grepl("^Calle ", NOMBRE), "CALLE", "PASAJE"),
         longitud_calle = length(geom)) %>% 
  rename(geometry = geom) %>% 
  group_by(tipo_c) %>% 
  summarise(longitud_tot = sum(longitud_calle),
            .groups = "drop") %>%
  mutate(tipo_ratio = longitud_tot / sum(longitud_tot),
         tipo_ratio = round(tipo_ratio,2),
         barrio = "Barrio Mugica",
         longitud_tot = longitud_tot)
  

#TIPO/ANCHO DE CALLES EN ST
callejero_sp = st_intersection(callejero, sectores_contorno) %>% 
  mutate(tipo_c = as.factor(tipo_c),
         longitud_calle = length(geometry)) %>% 
  group_by(sector, tipo_c) %>% 
  summarise(n_tipo = n(),
            longitud_tot = sum(longitud_calle),
            .groups = "drop") %>%
  group_by(sector) %>%
  mutate(tipo_ratio = longitud_tot / sum(longitud_tot),
         tipo_ratio = round(tipo_ratio,2),
         barrio = paste0("Sector Proximo ", sector),
         longitud_tot = longitud_tot) %>%
  ungroup()

tipo_calles = bind_rows(callejero_sp, callejero_bm) 

```
</details>

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
tabla =
datatable(tipo_calles%>% 
            select(barrio, tipo_c, longitud_tot, tipo_ratio) %>% 
            st_drop_geometry(), 
  style = 'bootstrap', 
  class = 'cell-border stripe',
  options = list(
    searching = FALSE,
    rownames = FALSE,
    pageLength = nrow(tipo_calles),   # ← show all rows
    dom = 't',                         # ← no search, no pagination, no length menu
    scrollY = FALSE,
    language = list(zeroRecords = "No existen datos para los requerimientos seleccionados"),
    columnDefs = list(
      list(targets = 0, visible = FALSE), # hide column 0
      list(targets = c(1:4), width = '150px', className = 'dt-left'))
    )
  )

tabla

```

<h2>
  SEGURIDAD </br>
  <small class="text-muted"> |  Delitos en 2023 </small>
</h2>


<details><summary> Código </summary>
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

#LLAMO LA BASE DE DELITOS Y GUARDO SOLAMENTE LOS PUNTOS QUE ME INTERESAN PARA LLAMARLO LUEGO SIN TENER UNA BASE TAN GRANDE:
delitos = read_excel(
  "data/delitos_2023.xlsx",
  col_names = TRUE,
  col_types = NULL,
  na = "",
  trim_ws = TRUE,
  skip = 0) %>% 
  clean_names()%>%
  mutate(
    latitud = as.numeric(latitud),
    longitud = as.numeric(longitud),
    latitud = ifelse(abs(latitud) > 1000, latitud / 1e6, latitud),
    longitud = ifelse(abs(longitud) > 1000, longitud / 1e6, longitud)
  )

delitos_caba = delitos %>%
  filter(!is.na(latitud), !is.na(longitud), longitud!="NULL", latitud!="NULL", latitud!=0) %>% 
  st_as_sf(coords = c("longitud", "latitud"), crs = 4326)%>% 
  st_transform(22185)

  
delitos_sp = st_intersection(delitos_caba, isocronas15_sp) %>% 
  clean_names() %>% 
  mutate(tipo = as.factor(tipo))


delitos_bm = st_intersection(delitos_caba,isocronas15_bm)%>% 
  clean_names()%>% 
  mutate(tipo = as.factor(tipo))

#"Amenazas","Hurto","Lesiones","Robo","Vialidad"

rm(delitos,delitos_caba)

delitos_sp_n = delitos_sp %>% 
  group_by(id_punto,sector,tipo) %>% 
  summarise(cantidad=n())

delitos_bm_n = delitos_bm %>% 
  group_by(id_punto,tipo) %>% 
  summarise(cantidad=n())

```
</details>



<details><summary> Código </summary>
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}



```
</details>

ggplot(b_buffer) +
  geom_sf()
