---
title: "Metodología de investigación"
description: |
  Caso de estudio: Barrio Padre Carlos Mugica y su entorno formal
---

  
```{r librerias, include=FALSE}
options(scipen = 999)

# Ver dónde está instalado Rcpp actualmente
find.package("Rcpp")

# Ver todas las rutas de librerías
.libPaths()

library(areal)
library(distill)
library(dplyr)
library(DT)
library(effsize)
library(ggmap)
library(ggplot2)
library(gotop)
library(htmltools)
library(janitor)
library(knitr)
library(leaflet)
library(leaflet.extras)
library(lwgeom)
library(openrouteservice)
library(osmdata)
library(osrm)
library(purrr)
library(Rcpp)
library(readxl)
library(rmarkdown)
library(sf)
library(stringr)
library(tidyr)
library(tidyverse)

#In RStudio, you can render your site locally (knit + preview all .Rmd files in one fell swoop). Se puede escribir en la consola: rmarkdown::render_site()  SIEMPRE HACER ESTO PARA IMPACTAR LOS CAMBIOS EN EL SITIO!

```

```{r exclude_false, include=FALSE}
#PARA OCULTAR CHUNKS DE CODIGO
knitr::opts_chunk$set(
  comment =FALSE,
  #fig.width = 6, fig.height = 6, 
  message=FALSE, warning=FALSE
)
```

```{r, include=FALSE}
#SIMBOLO PARA SUBIR HASTA ARRIBA EN EL SITIO
gotop::use_gotop(
  src = "fas fa-chevron-circle-up", # css class from Font Awesome
  color = "tomato", # color
  opacity = 0.8, # transparency
  width = 30, # size
  appear = 100 # number of pixels before appearance
  )
```
<script>
function initTableSettings() {
  $('.searchable-table').each(function () {  // Only apply to tables with "searchable-table" class
    var table = $(this).DataTable();
    table.columns().every(function () {
      var column = this;
      var header = $(column.header());
      var input = $('<br><input type="text" placeholder="Filter" style="width: 75%;"/>').appendTo(header).on('keyup change', function () {
        if (column.search() !== this.value) {
          column.search(this.value).draw();
        }
      });
    });
  });
}

$(document).ready(function() {
  initTableSettings();
});
</script>

En la Ciudad Autónoma de Buenos Aires, las villas y asentamientos informales presentan distintos grados de integración al tejido urbano formal. Mientras algunos se encuentran físicamente insertos en zonas consolidadas, con mayor conectividad vial y cercanía a nodos comerciales, otros se desarrollan en condiciones de mayor aislamiento o fragmentación, producto de barreras físicas urbanas. Ésta heterogeneidad territorial genera grandes diferencias en el acceso efectivo a bienes y servicios básicos, incluyendo la oferta de alimentos, cuya disponibilidad, diversidad y precio pueden variar sensiblemente según el grado de integración urbana del barrio.

Desde esta perspectiva, resulta pertinente preguntarse en qué medida el costo de los insumos alimentarios en barrios populares está condicionado por factores como la accesibilidad física, la presencia de circuitos de comercialización formales o informales, el nivel de conectividad con zonas de mayor actividad económica, y las condiciones de seguridad o infraestructura urbana. Este trabajo buscará abordar dicha problemática a través del caso del Barrio Padre Carlos Mugica, combinando análisis espacial y recolección de datos cuantitativos para identificar patrones y correlaciones que permitan una mejor comprensión del fenómeno y orienten intervenciones de política pública en materia alimentaria y urbana.

La estrategia metodológica adoptada en este estudio combina un enfoque cuantitativo-descriptivo con herramientas de análisis territorial. En primer lugar, se delimita un recorte espacial que comprende el Barrio Padre Carlos Mugica y cuatro sectores próximos, seleccionados por su carácter predominantemente residencial y su cercanía geográfica. En cada uno de estos territorios se relevaron los precios de seis productos alimentarios representativos de la canasta básica, seleccionados por su consumo cotidiano y su peso en la dieta habitual de los hogares. A partir de estos datos se calcula un valor medio de una “sub-canasta básica” alimentaria para el barrio Mugica y para cada uno de los sectores de comparación.

La intención de este análisis es evaluar las diferencias relativas de precios entre los distintos sectores y caracterizar la oferta alimentaria disponible para los residentes del barrio. A su vez, la información obtenida es contrastada con una serie de variables contextuales, tales como la densidad de locales comerciales, la densidad poblacional, los niveles de accesibilidad y conectividad vial, la irrigación de transporte público, indicadores de seguridad, y otros factores urbanos y socioeconómicos relevantes que permitan explorar posibles relaciones causales entre el entorno urbano y el costo de acceso a los alimentos.

Finalmente, a partir de los resultados obtenidos, se busca esbozar propuestas de intervención —desde el ámbito público, privado o comunitario— orientadas a reducir las distorsiones o inequidades observadas en el mercado alimentario local, y contribuir así a mejorar las condiciones de vida de los habitantes del Barrio Mugica desde una perspectiva territorial.


<h2>
  LA UNIDAD TERRITORIAL </br>
  <small class="text-muted"> |  Recorte y selección </small>
</h2>

Para comenzar a definir la unidad ed análisis, se considera polígono del Barrio Padre Carlos Mugica y el de la ciudad de Buenos Aires.

Como el objetivo es observar el acceso a la oferta alimentaria que tiene el Barrio Mugica en comparación a la ciudad formal circundante, se plantea un buffer a 1500 metros del Barrio Mugica, y se seleccionan así los radios censales (censo 2022) que se intersectan con el mismo.

<details><summary> Código </summary> 
## Barrio 

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
#CARGO LAS BASES
ciudad = st_read("data/perimetro.geojson") %>% 
  #mutate(AREAKM2 = round((AREA/1000000),2)) %>% 
  st_transform(22185)%>% 
  st_make_valid()

b_mugica_sectores = st_read("data/sector.shp")

b_mugica <- st_buffer(st_union(b_mugica_sectores), 0.5) %>%
  st_sf() %>%  # convert to sf object with geometry column
  st_transform(22185) %>%
  mutate(area_km2 = as.numeric(st_area(geometry)) / 1e6)


#DEFINO 2 FUNCIONES PARA TRANSFORMAR LAS BASES ENTRE 4326 Y 22185 SEGUN SI TENGO QUE HACER ANÁLISIS GEOGRÁFICO O SI TENGO QUE MAPEAR:
transformar_todo_a_crs <- function(crs_destino = 4326) {
  objetos <- ls(envir = .GlobalEnv) 
  for (nombre in objetos) {
    objeto <- get(nombre, envir = .GlobalEnv) 
    if (inherits(objeto, "sf")) {
      nuevo <- st_transform(objeto, crs_destino)
      assign(nombre, nuevo, envir = .GlobalEnv)
    } 
  } 
}

```
## Buffer

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
#CREO UN BUFFER EN TORNO AL BARRIO PARA PODER SELECCIONAR LOS RADIOS CENSALES PRÓXIMOS:
b_buffer <- st_buffer(b_mugica, dist = 1500) %>% 
  st_transform(22185)

#LLAMO Y FILTRO LOS RADIOS CENSALES EN BASE A ESE BUFFER. LO HAGO UNA SOLA VEZ Y GUARDO PARA NO REPETIR NI CARGAR BASES MUY PESADAS:

#radios_censales = st_read("data/radios_censales.shp", options = "ENCODING=LATIN1")%>% 
#                    st_transform(22185) %>% 
#  filter(jur == "Ciudad Autónoma De Buenos Aires")

#st_write(radios_censales, "data/radios_censales_filtrados.geojson")

radios_censales = st_read("data/radios_censales_filtrados.geojson")%>% 
  st_transform(22185)

b_buffer = st_intersection(b_buffer,ciudad)

radios_buffer = st_filter(radios_censales, b_buffer) %>% 
  filter(!grepl("50480|50479|72485",id))

rm(radios_censales)

```
</details>

Imagen 1. Ciudad Autónoma de Buenos Aires, polígono del Barrio Padre Carlos Mugica definido en la ley N° 6.129 (2018) y buffer de 1500m en torno al mismo.

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
transformar_todo_a_crs(4326)

mapa =
leaflet(st_zm(ciudad)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("CABA","Buffer Barrio Mugica","Barrio Mugica" ))%>% 
  
  addPolygons(data= ciudad,
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "CABA") %>% 

  addPolygons(data= b_buffer,
              color = "black",
              weight = 0.6,
              opacity = 0.7,
              fillColor = "#a64d79",
              fillOpacity = 0.3,
              popup = "<strong>BUFFER BARRIO - DISTANCIA:</strong>1500m",
              group = "Buffer Barrio Mugica") %>% 

  
  addPolygons(data= b_mugica,
              color = "black",
              weight = 0.8,
              opacity = 0.7,
              fillColor = "#a64d79",
              fillOpacity = 0.4,
              popup = ~paste("<strong>BARRIO P. MUGICA - AREA:</strong>", round(area_km2, 3), "km2"),
              group = "Barrio Mugica")  


    
mapa

#transformar_todo_a_crs(22185)
```

Imagen 2. Radios Censales comprendidos dentro del buffer. 

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
#transformar_todo_a_crs(4326)

mapa =
leaflet(st_zm(ciudad)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("Radios Censales Primer Filtro","CABA","Barrio Mugica", "Buffer Barrio Mugica" ))%>% 
  
  addPolygons(data= ciudad,
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "CABA") %>% 
  
  addPolygons(data= b_mugica,
              color = "black",
              weight = 0.8,
              opacity = 0.7,
              fillColor = "#a64d79",
              fillOpacity = 0.4,
              popup = ~paste("<strong>AREA KM2:</strong>", round(area_km2, 3)),
              group = "Barrio Mugica") %>% 


  addPolygons(data= radios_buffer,
              color = "black",
              weight = 0.6,
              opacity = 0.7,
              fillColor = "#a2c4c9",
              fillOpacity = 0.5,
              popup = ~paste("<strong> ID: </strong>", id),
              group = "Radios Censales Primer Filtro") %>% 

 addPolygons(data= b_buffer,
              color = "black",
              weight = 0.6,
              opacity = 0.7,
              fillColor = "#a64d79",
              fillOpacity = 0.3,
              popup = "<strong>BUFFER BARRIO - DISTANCIA:</strong>1500m",
              group = "Buffer Barrio Mugica")
mapa

transformar_todo_a_crs(22185)
```

En base a este primer recorte, se excluyen ciertos radios donde no predomina el uso residencial, ya que la intención es observar el acceso que tienen las personas desde sus viviendas a los comercios. 
Los resultantes serán, por un lado, los radios comprendidos dentro del polígono del Barrio Mugica; y el otro, los sectores que cuentan con la proximidad suficiente como para considerarse el entorno urbano formal al que debe integrarse la urbanización informal.
Para filtrar los radios censales se define una densidad mínima de 3500v/km2 (viviendas por kilómetro cuadrado).

El mapa resultante es el siguiente:

<details><summary> Código </summary>
## Radios Censales y Densidad de viviendas

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
#EXCLUSIONES
exclusion = read_excel("data/exclusion_radios.xlsx")

exclu_id = na.omit(exclusion$id) 
                   
#INCORPORO LA INFORMACION SOBRE VIVIENDAS DE LA BASE DE RADIOS CENSALES:
viviendas = read_excel("data/_tmp_11601061.xlsx", sheet = "data") %>% 
  rename("viviendas" = "C1", "cod_indec" = "Código") %>% 
  mutate(cod_indec = as.character(cod_indec),
         cod_indec = paste0("0",cod_indec))

#FILTRO POR DENSIDAD DE VIVIENDAS:
radios_viviendas = left_join(radios_buffer, viviendas, by = "cod_indec") %>% 
  mutate(area_km2 = round(as.numeric(st_area(geometry)) / 1e6,4),
         densidad_viviendas = round(viviendas/area_km2,2)) %>% 
  filter(densidad_viviendas > 4427,
         densidad_viviendas < 60462) %>%
  filter(!id %in% exclu_id)

#INCORPORO INFORMACION DE VIVIENDAS A LA BASE DEL BARRIO
radios_censales_bm = st_filter(radios_viviendas, b_mugica) %>% 
  st_drop_geometry() %>% 
  select("cod_indec") %>%
  mutate(formal = 0,
         sector = "0")

#UNO AMBAS BASES Y DEFINO SI LOS RADIOS PERTENECEN A UN SECTOR FORMAL O INFORMAL
radios_censales_tot = left_join(radios_viviendas, radios_censales_bm, by = "cod_indec") %>% 
  mutate(formal = if_else(is.na(formal), 1, formal))

 

#FILTRO LOS RADIOS CENSALES FORMALES
radios_censales_sp = radios_censales_tot %>% 
  filter(formal == 1) %>% 
  mutate(cod_indec_num = as.numeric(cod_indec)) %>% 
  filter(cod_indec_num > 20070400,
         cod_indec_num < 20141605)%>%
  arrange(id) #%>% 
  #mutate(sector = as.numeric(ceiling(row_number() / 36.5)))

#CALCULO EL AREA TOTAL DE LOS RADIOS FORMALES y LA DIVIDO POR LA SUPERFICIE DEL BARRIO PARA SABER CUANTOS SECTORES PROXIMOS DEFINIR:
radios_censales_sp %>% 
  summarise(area_km2=sum(area_km2))/b_mugica$area_km2

#OBTENGO LOS CENTROIDES Y SUS COORDENADAS
centros = st_centroid(radios_censales_sp) %>% 
  st_coordinates()

# APLICO K MEANS PARA AGRUPAR LOS RADIOS CENSALES EN 3 SECTORES SEGUN PROXIMIDAD
set.seed(123)  # Para reproducibilidad
kmeans_result = kmeans(centros, centers = 3)

# AGREGO EL ID DEL SECTOR A LA BASE
radios_censales_sp$sector = as.numeric(kmeans_result$cluster)

radios_censales_bm = radios_censales_tot %>% 
  filter(formal == 0)

#UNIFICO LAS BASES DE NUEVO
radios_censales_tot = bind_rows(radios_censales_sp,radios_censales_bm %>% 
                                  mutate(sector = as.numeric(sector))) %>% 
  select(-c(cde,dpto,jur,cpr,fid,cfn,cro,tro,sag,)) %>% 
  rename(id_radio = id,
         area_km2_r = area_km2,
         geometry_radio = geometry) %>% 
  arrange(sector)

sectores_tot = radios_censales_tot %>% 
  group_by(sector) %>% 
  summarise(viviendas = sum(viviendas),
            area_km2_s = sum(area_km2_r),
            densidad_viviendas = viviendas/area_km2_s)%>% 
  arrange(sector) %>% 
  rename("geometry" = geometry_radio)


#ISOCRONAS SECTORES PROXIMOS
contorno_sp = sectores_tot %>% 
  filter(sector!="0") %>% 
  group_by(sector) %>% 
  summarise(area_km2_s)

#ELIMINO BASES EN DESUSO
#rm(radios_buffer, radios_viviendas, viviendas, b_buffer)
```
</details>

Para proseguir con el análisis se proponen unidades territoriales intermedias definiendo de esta manera 3 "sectores próximos" a comparar con el Barrio Mugica (BM).
Para ello se agrupan los radios censales de tal manera que cada sector tenga una superficie similar a la del BM.

Imagen 5. Radios censales seleccionados agrupados en:
  
  * 1 sector informal: el Barrio Mugica, y 
  * 4 sectores formales: Los "sectores próximos" numerados 01 a 04.

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

pal = colorNumeric(
  palette = colorRampPalette(c("#a2c4c9", "#134f5c"))(100),
  domain = radios_censales_sp$sector
)

transformar_todo_a_crs(4326)

mapa =
leaflet(st_zm(sectores_tot)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("Sectores Proximos","Barrio Mugica"))%>% 
  
  addPolygons(data= radios_censales_bm,
              color = "black",
              weight = 1,
              opacity = 0.3,
              fillColor = "#a64d79",
              fillOpacity = 0.5,
              popup = ~paste("<strong> ID: </strong>", id,"<br><strong>DENSIDAD VIVIENDAS:</strong>", densidad_viviendas,"<br><strong>FORMAL:</strong>", formal),
              group = "Barrio Mugica") %>%

  addPolygons(data= radios_censales_sp,
              color = "black",
              weight = 0.6,
              opacity = 0.3,
              fillColor = ~pal(sector),
              fillOpacity = 0.5,
              popup = ~paste("<strong> ID: </strong>", id,"<br><strong>DENSIDAD VIVIENDAS:</strong>", densidad_viviendas,"<br><strong>FORMAL:</strong>", formal,"<br><strong>Sector:</strong>", sector),
              group = "Sectores Proximos") 

mapa

transformar_todo_a_crs(22185)
```

<h2>
  ACCESIBILIDAD </br>
  <small class="text-muted"> |  Áreas accesibles a pie </small>
</h2>

Para estudiar el acceso que tiene la población a los locales comerciales de venta de alimentos, se tiene en cuenta el traslado a pie.

  En pos de delimitar el análisis, se produce una muestra para representar a los habitantes en el formato de 9 puntos aleatorios por sector, desde los cuales se calculan isocronas de 5 y 10 minutos.
  
<small style="color: #a64d79;"> [ Justificar selección de "a pie" como medio de transporte y distancias medidas en minutos considerando que son compras de alimentos cotidianos y usos/costumbres comunes para los habitantes de la ciudad. (Fuentes posibles: Ciudades de 15m de Moreno (suboptima para mi gusto), https://pmc.ncbi.nlm.nih.gov/articles/PMC9331805/ o https://www.cooperativaciencia.cl/salud/2024/10/28/periferia-de-la-region-metropolitana-concentra-la-mayor-cantidad-de-desiertos-alimentarios/) Estudiar y desarrollar ] </small>

<details><summary> Código </summary>
## PUNTOS ALEATORIOS
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

# PUNTOS ALEATORIOS --------------------------------------------------------------

#GENERO Y GUARDO LOS PTOS ALEATORIOS PORQUE SINO CADA VEZ QUE CORRO EL CODIGO, SON NUEVOS:
#puntos_aleatorios_tot = sectores_tot %>%
#  rowwise() %>%
#  mutate(puntos = list(st_sample(geometry, size = 100, type = "random"))) %>%
#  ungroup() %>%
#  select(-geometry) %>%  # elimina la geometría original
#  unnest(puntos) %>%
#  st_as_sf(coords = "puntos", crs = st_crs(sectores_tot)) %>% 
#  st_drop_geometry() %>% 
#  rename("geometry"="puntos") %>% 
#  mutate(id_punto = paste0(sector,"_",row_number()))

#GUARDO LOS PUNTOS PARA LLAMARLOS DESPUES:
#st_write(puntos_aleatorios_tot, "data/puntos_aleatorios_tot.geojson")

#LLAMO A LOS PUNTOS ALEATORIOS GUARDADOS DE LOS SECTORES BM Y SP:
puntos_aleatorios_tot = st_read("data/puntos_aleatorios_tot.geojson") %>% 
  st_transform(4326) %>% 
  clean_names() %>% 
  mutate(sector=as.numeric(sector))%>% 
  mutate(nombre_sector = if_else(sector == "0", "Barrio Mugica", paste0("Sector Próximo ", sector)))
                    
```

## ISOCRONAS
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

#ARMO LAS ISOCRONAS DE 5 Y 10 MIN 1 SOLA VEZ--------------------------------------------------

# Configurar API key
#ors_api_key("eyJvcmciOiI1YjNjZTM1OTc4NTExMTAwMDFjZjYyNDgiLCJpZCI6IjZkOGRlYWNiYWQ1NTQwNzlhZTdlZDUzODM1OWY0NTMxIiwiaCI6Im11cm11cjY0In0=")

# Construir isocronas de 5 minutos
#lista_isocronas_tot_05 <- lapply(seq_len(nrow(puntos_aleatorios_tot)), function(i) {
#  pt     <- puntos_aleatorios_tot[i, ]
#  coords <- sf::st_coordinates(pt)
  
# Sys.sleep(0.5)  # respetar límite de la API gratuita
  
#  iso <- tryCatch(
#    ors_isochrones(
#      locations  = list(c(coords[, "X"], coords[, "Y"])),
#      profile    = "foot-walking",
#      range      = 300,         # 5 minutos en segundos
#      range_type = "time",
#      output     = "sf"
#    ),
#    error   = function(e) { message("Error en ", pt$id_punto, ": ", e$message); return(NULL) },
#    warning = function(w) { message("Warning en ", pt$id_punto, ": ", w$message); return(NULL) }
#  )
  
#  if (is.null(iso) || nrow(iso) == 0) return(NULL)
  
#  mutate(iso, id_punto = pt$id_punto, sector = pt$sector)
#})

# Eliminar NULLs
#lista_isocronas_tot_05_2 <- Filter(Negate(is.null), lista_isocronas_tot_05)

# Combinar todo en un solo sf
#isocronas_tot_05 <- do.call(bind_rows(lista_isocronas_tot_05_2)

# Combine sf rows
#isocronas05_tot = do.call(rbind, lista_isocronas_tot_05_2)
# Ver qué columnas son listas
#sapply(isocronas05_tot, class)

#st_write(isocronas05_tot, "data/isocronas05_tot.geojson")


#LLAMO LAS BASES GUARDADAS------------------------------------------------------------


isocronas05_tot = st_read("data/isocronas05_tot.geojson")%>% 
                    st_transform(22185) %>% 
                    st_make_valid() %>% 
                    mutate(nombre_sector=if_else(sector == 0, "Barrio Mugica", paste0("Sector Próximo ", as.character(sector))))

isocronas10_tot = st_read("data/isocronas10_tot.geojson")%>% 
                    st_transform(22185) %>% 
                    st_make_valid() %>% 
                    mutate(nombre_sector=if_else(sector == "0", "Barrio Mugica", paste0("Sector Próximo ", as.character(sector))))


```

  
</details>

Imagen 6. Isocronas de 5 minutos a pie desde los puntos aleatotios dentro del Barrio Mugica y de cada uno de los 4 sectores próximos.

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

transformar_todo_a_crs(4326)

mapa =
leaflet(st_zm(sectores_tot)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("Isocronas Barrio Mugica","Barrio Mugica", "Puntos Aleatorios BM", "Isocronas Sector 1", "Isocronas Sector 2", "Isocronas Sector 3", "Isocronas Sector 4", "Sectores Proximos", "Puntos Aleatorios SP"))%>% 
  
  addPolygons(data= isocronas05_tot,
              color = "#a64d79",
              weight = 1,
              opacity = 0.7,
              fillColor = "#a64d79",
              fillOpacity = 0.4,
              group = "Isocronas Barrio Mugica") %>%
  

  addPolygons(data= b_mugica,
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Barrio Mugica") %>%

  addPolygons(data= isocronas05_tot %>% filter(sector=="1"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 1") %>% 
  
    addPolygons(data= isocronas05_tot%>% filter(sector=="2"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 2") %>% 
  
    addPolygons(data= isocronas05_tot%>% filter(sector=="3"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 3") %>% 
  
    addPolygons(data= isocronas05_tot %>% filter(sector=="4"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 4") %>% 

  addPolygons(data= contorno_sp,
              color = "black",
              weight = 0.6,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Sectores Proximos") %>% 
  
    addCircleMarkers(data=puntos_aleatorios_tot,
              fillColor = "black",
              weight = 0.5,
              color = "black",
              fillOpacity = 0.5,
              radius = 2,
              popup = ~paste("<strong> ID_punto: </strong>", id_punto),
              group = "Puntos Aleatorios BM") %>%

  addCircleMarkers(data=puntos_aleatorios_tot,
              fillColor = "black",
              weight = 0.5,
              color = "black",
              fillOpacity = 0.5,
              radius = 2,
              popup = ~paste("<strong> ID_punto: </strong>", id_punto),
              group = "Puntos Aleatorios SP")

mapa

```

Imagen 6. Isocronas de 10 minutos a pie desde los puntos aleatorios dentro del Barrio Mugica y de cada uno de los 4 sectores próximos.

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

mapa =
leaflet(st_zm(sectores_tot)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("Isocronas Barrio Mugica","Barrio Mugica", "Puntos Aleatorios BM", "Isocronas Sector 1", "Isocronas Sector 2", "Isocronas Sector 3", "Isocronas Sector 4", "Sectores Proximos", "Puntos Aleatorios SP"))%>% 
  
  addPolygons(data= isocronas10_tot,
              color = "#a64d79",
              weight = 1,
              opacity = 0.7,
              fillColor = "#a64d79",
              fillOpacity = 0.4,
              group = "Isocronas Barrio Mugica") %>%
  

  addPolygons(data= b_mugica,
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Barrio Mugica") %>%

  addPolygons(data= isocronas10_tot %>% filter(sector=="1"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 1") %>% 
  
    addPolygons(data= isocronas10_tot%>% filter(sector=="2"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 2") %>% 
  
    addPolygons(data= isocronas10_tot%>% filter(sector=="3"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 3") %>% 
  
    addPolygons(data= isocronas10_tot %>% filter(sector=="4"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 4") %>% 

  addPolygons(data= contorno_sp,
              color = "black",
              weight = 0.6,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Sectores Proximos") %>% 
  
    addCircleMarkers(data=puntos_aleatorios_tot,
              fillColor = "black",
              weight = 0.5,
              color = "black",
              fillOpacity = 0.5,
              radius = 2,
              popup = ~paste("<strong> ID_punto: </strong>", id_punto),
              group = "Puntos Aleatorios BM") %>%

  addCircleMarkers(data=puntos_aleatorios_tot,
              fillColor = "black",
              weight = 0.5,
              color = "black",
              fillOpacity = 0.5,
              radius = 2,
              popup = ~paste("<strong> ID_punto: </strong>", id_punto),
              group = "Puntos Aleatorios SP")

mapa

transformar_todo_a_crs(22185)
```

<h2>
  LOCALES COMERCIALES </br>
  <small class="text-muted"> |  Dentro de los polígonos </small>
</h2>

Teniendo definidos los puntos de origen que representan a los consumidores y sus áreas de infuencia, se considera la oferta ubicada dentro de las mismas.

El relevamiento de los comercios se realizó en junio de 2025.

Se calculó el tamaño de la muestra necesaria para un nivel de confianza del 95% y un margen de error del 10%. 
En el Barrio Mugica, teniendo en cuenta el tamaño del universo (518 comercios que comprenden los rubros: Almacén, Kiosco, Carnicería, Fiambrería y Verdulería. Base territorial 2023), se relevaron 82 comercios. 

En los sectores formales, se calculó el tamaño de la muestra basándose en un universo de N comercios relevados en el territorio y complementados con una revisión de establecimientos registrados en Google Maps, y de los no registrados, pero visibles por medio de Google Street View. En consecuencia, la muestra está compuesta por n observaciones, permitiendo un nivel de confianza del nn% y un margen de error del nn%.

<small style="color: #a64d79;"> [ Completar esto una vez que termine de pasar los relevamientos de los sectores próximos y tenga esa base limpia. Puede que me convenga expresarlo en m de calle relevada sobre m totales por sector en vez de cantidad de comercios relevados sobre comercios existentes...revisar ] </small>

Comercios de proximidad dentro del Barrio Mugica

<details><summary> Código </summary>
## Comercios BM
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

#HAGO ESTO UNA VEZ PARA SIMPLIFICAR EL PROCESO-----------------------------------------

#VEO QUE CAPAS TENGO EN INFORMACION TERRITORIAL, NO LLAMAR TODO.
#print(st_layers("data/informacion_territorial.gpkg"))

#ME TRAIGO LA CAPA DE COMERCIOS Y LA FILTRO SEGÚN LOS QUE CORRESPONDEN A LA INVESTIGACIÓN.
#comercios1_bm = st_read("data/informacion_territorial.gpkg", layer = "comercios") %>% 
#  filter(Tipo == "Comercio",
#         grepl("ALMACÉN|Kiosko|Carnicer|Fiambrería|Verdulería", Rubro),
#         !st_is_empty(geom)) %>% 
#  st_transform(22185) %>% 
#  mutate(Altura = as.numeric(Altura)) %>% 
#  rename(geometry = geom) %>% 
#  arrange(Calle, Altura) %>% 
#  clean_names() %>% 
#  select(direccion)


#GUARDO LAS OBSERVACIONES PARA IR A RELEVAR AL TERRITORIO INCORPORANDO LA INFORMACIÓN Y ACTUALIZANDO LOS DATOS YA QUE SON DEL 2022
#st_write(comercios1_bm,"data/comercios1_bm.xlsx")
#comercios_sin_geom = read_excel("data/comercios_bm_sin_geom.xlsx") %>%
#  clean_names()

#comercios_sin_geom$geometry <- st_as_sfc(comercios_sin_geom$geometry)
 
#comercios_sin_geom = comercios_sin_geom %>% 
#  st_as_sf(crs = 4326) %>% 
#  st_transform(22185)

#comercios2_bm = rbind(comercios1_bm, comercios_sin_geom)

#comercios_bm_territorio = read_excel("data/comercios_bm_territorio.xlsx") %>%
#  clean_names()

#comercios_bm_cadena = comercios_bm_territorio %>% 
#  filter(cadena == "si") 

#comercios_bm_cadena$geometry <- st_as_sfc(comercios_bm_cadena$geometry)

#comercios_bm_cadena = comercios_bm_cadena %>% 
#                      st_as_sf(crs = 4326) %>% 
#                      st_transform(22185)


#comercios_bm_local = comercios_bm_territorio %>% 
#  filter(cadena == "no") %>% 
#  select(-geometry)
  

#comercios_bm_local_geom = left_join(comercios_bm_local, comercios2_bm, by = #"direccion") %>% 
#  mutate(relevado = as.factor(if_else(is.na(leche), 0, 1))) %>% 
#  st_as_sf() %>% 
#  st_transform(22185)


#comercios_bm = rbind(comercios_bm_local_geom, comercios_bm_cadena) %>% 
#  filter(!st_is_empty(geometry)) %>% 
#  mutate(id_comercio = paste0("cbm_", str_pad(01:(00 + n()), width = 4, pad = "0")))

#st_write(comercios_bm,"data/comercios_bm.gpkg")

#rm(comercios1_bm, comercios_sin_geom, comercios2_bm, comercios_bm_cadena, comercios_bm_local, comercios_bm_territorio, comercios_bm_local_geom )
#-----------------------------------------------------------------------------------

# Habiendo hecho todo lo de arriba, ahora solo llamo la base de comercios del bm
comercios_bm = st_read("data/comercios_bm.gpkg") %>% 
  st_transform(22185) %>% 
  rename(geometry = geom) %>% 
  mutate(tanda = 0)

```
## Comercios SP
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
# SECTORES PROXIMOS

comercios_sp = read_excel("data/comercios_territorio_sp.xlsx", 
                           sheet = "datos_limpios") %>%
  clean_names() %>%
  mutate(across(c(leche, harina, azucar, arroz, papa, fiambre_jc, carne_pic),
                ~ if_else(. == 0, NA_real_, .))) %>% 
  filter(!is.na(id_comercio))
  

comercios_sp$geometry <- st_as_sfc(comercios_sp$geometry)

comercios_sp = comercios_sp %>% 
                st_as_sf(crs = 4326) %>% 
                st_transform(22185)

comercios_tot = bind_rows(comercios_sp, comercios_bm) %>% 
  select(-c(fecha_hora_cest,marca_temp_cest,link,calle,altura,direccion,cuales_son_los_principales_factores_que_encarecen_los_productos_que_vendes,dificultades_para_abastecerse_otros_comentarios,relevado,numero_real))
```

</details>

Imagen 7. Comercios relevados dentro del sector informal y los sectores formales próximos.

En el mapa se pueden observar:

  * En la capa de los comercios, al hacer click sobre los puntos: Los rubros de los establecimientos, y si son franquicias de cadenas o no.
  * Los sectores y sus puntos aleatorios.
  * Las isocronas de 5minutos a partir de cada punto.
  

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

transformar_todo_a_crs(4326)

mapa =
leaflet(st_zm(sectores_tot)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("Barrio Mugica", "Isocronas BM", "Isocronas Sector 1", "Isocronas Sector 2", "Isocronas Sector 3", "Isocronas Sector 4", "Sectores Proximos", "Puntos Aleatorios SP", "Comercios Tandas 1 y 2", "Comercios Tandas 3 y 4"))%>% 
  


  addPolygons(data= b_mugica,
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Barrio Mugica") %>%
  
  
    addPolygons(data= isocronas05_tot,
              color = "#a64d79",
              weight = 0.6,
              opacity = 0.7,
              fillColor = "#a64d79",
              fillOpacity = 0.4,
              group = "Isocronas BM") %>% 

  addPolygons(data= isocronas05_tot %>% filter(sector=="1"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 1") %>% 
  
    addPolygons(data= isocronas05_tot %>% filter(sector=="2"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 2") %>% 
  
    addPolygons(data= isocronas05_tot %>% filter(sector=="3"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              fillOpacity = 0.4,
              group = "Isocronas Sector 3") %>% 
  
    addPolygons(data= isocronas05_tot %>% filter(sector=="4"),
              color = ~pal(sector),
              weight = 0.6,
              opacity = 0.7,
              fillColor = ~pal(sector),
              popup = ~paste("<strong>ID:</strong>", id_punto),
              fillOpacity = 0.4,
              group = "Isocronas Sector 4") %>%  

  addPolygons(data= sectores_tot,
              color = "black",
              weight = 0.6,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Sectores Proximos") %>% 
  

  addCircleMarkers(data=puntos_aleatorios_tot,
              fillColor = "black",
              weight = 0.5,
              color = "black",
              fillOpacity = 0.5,
              popup = ~paste("<strong>ID:</strong>", id_punto),
              radius = 2,
              group = "Puntos Aleatorios SP") %>% 
  
  
  addCircleMarkers(data = comercios_tot,
              fillColor = "#f6b26b",
              weight = 2,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              popup = ~paste("<strong>ID:</strong>", id_comercio, "</br> <strong>RUBRO:</strong>", rubro, "</br><strong>CADENA:</strong>", cadena, "</br><strong>RELEVADO:</strong>", id_direcciones ),
              group = "Comercios")


 
mapa

transformar_todo_a_crs(22185)
```

<details><summary> Código </summary>
## Comercios por isocrona 5min
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

# COMERCIOS POR ISOCRONA
comercios_iso = st_join(comercios_tot, isocronas05_tot, join = st_within) %>%
  filter(!is.na(id_punto)) 

comercios_iso_sum = comercios_iso %>% 
  group_by(nombre_sector,id_punto) %>% 
  summarise('Total Comercios' = n(),
    'Comercios cadena'    = sum(cadena == "si", na.rm = TRUE),
    'Comercios no cadena' = sum(cadena == "no", na.rm = TRUE)) %>% 
  st_drop_geometry() %>% 
  rename('Nombre Sector' = nombre_sector)

# Pasar a formato long
comercios_long <- comercios_iso_sum %>%
  pivot_longer(
    cols = c('Comercios cadena', 'Comercios no cadena'),
    names_to = "cadena",
    values_to = "n_comercios"          
  ) %>%
  mutate(
    `Nombre Sector` = as.factor(`Nombre Sector`),
    cadena = recode(cadena,
                    "Comercios cadena"    = "Cadena",      
                    "Comercios no cadena" = "No cadena")
  )
```
</details>

Tabla 1. Cantidad de comercios comprendidos dentro de las isocronas de 5min por sector.

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
tabla = datatable(
  comercios_iso_sum, 
  style = 'bootstrap', 
  class = 'cell-border stripe',
  options = list(
    searching = FALSE,
    rownames = FALSE,
    pageLength = -1,
    dom = 't',
    scrollY = '300px',
    language = list(zeroRecords = "No existen datos para los requerimientos seleccionados"),
    columnDefs = list(
      list(targets = 0, visible = FALSE),
      list(targets = c(1,2), width = '150px', className = 'dt-left')
    )
  )
)

tabla

```

Gráfico 1. Cantidad de comercios (cadena y no cadenas) por isocrona de 5min por sector.

```{r, echo=FALSE, out.width='90%', fig.align='left', out.height=400}

ggplot(comercios_long, aes(x = n_comercios, y = `Nombre Sector`, fill = cadena)) +
  geom_boxplot(outlier.shape = NA, alpha = 0.8) +
  scale_fill_brewer(palette = "Accent") +
  labs(x = "Cantidad de comercios por isocrona", y = "Sector", fill = "Tipo") +
  theme_minimal(base_size = 14) +
  theme_minimal(base_size = 14) +
  scale_x_continuous(limits = c(0, 110), breaks = seq(0, 110, by = 10))+
  theme(
    legend.position = "bottom",
    plot.title = element_blank(),
    axis.text.y = element_text(size = 10)
  )
```

<details><summary> Código </summary>
## Comercios por isocrona 10min
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

# COMERCIOS POR ISOCRONA
comercios_iso = st_join(comercios_tot, isocronas10_tot, join = st_within) %>%
  filter(!is.na(id_punto)) 

comercios_iso_sum = comercios_iso %>% 
  group_by(nombre_sector,id_punto) %>% 
  summarise('Total Comercios' = n(),
    'Comercios cadena'    = sum(cadena == "si", na.rm = TRUE),
    'Comercios no cadena' = sum(cadena == "no", na.rm = TRUE)) %>% 
  st_drop_geometry() %>% 
  rename('Nombre Sector' = nombre_sector)

# Pasar a formato long
comercios_long <- comercios_iso_sum %>%
  pivot_longer(
    cols = c('Comercios cadena', 'Comercios no cadena'),
    names_to = "cadena",
    values_to = "n_comercios"          # comillas, no backticks
  ) %>%
  mutate(
    `Nombre Sector` = as.factor(`Nombre Sector`),
    cadena = recode(cadena,
                    "Comercios cadena"    = "Cadena",      # debe coincidir exacto
                    "Comercios no cadena" = "No cadena")
  )
```
</details>

Tabla 1. Cantidad de comercios comprendidos dentro de las isocronas de 10min por sector.

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
tabla = datatable(
  comercios_iso_sum, 
  style = 'bootstrap', 
  class = 'cell-border stripe',
  options = list(
    searching = FALSE,
    rownames = FALSE,
    pageLength = -1,
    dom = 't',
    scrollY = '300px',
    language = list(zeroRecords = "No existen datos para los requerimientos seleccionados"),
    columnDefs = list(
      list(targets = 0, visible = FALSE),
      list(targets = c(1,2), width = '150px', className = 'dt-left')
    )
  )
)

tabla

```

Gráfico 1. Cantidad de comercios (cadena y no cadenas) por isocrona de 10min por sector.

```{r, echo=FALSE, out.width='90%', fig.align='left', out.height=400}

ggplot(comercios_long, aes(x = n_comercios, y = `Nombre Sector`, fill = cadena)) +
  geom_boxplot(outlier.shape = NA, alpha = 0.8) +
  scale_fill_brewer(palette = "Accent") +
  labs(x = "Cantidad de comercios por isocrona", y = "Sector", fill = "Tipo") +
  theme_minimal(base_size = 14) +
  theme_minimal(base_size = 14) +
  scale_x_continuous(limits = c(0, 370), breaks = seq(0, 370, by = 50))+
  theme(
    legend.position = "bottom",
    plot.title = element_blank(),
    axis.text.y = element_text(size = 10)
  )
```

<details><summary> Código </summary>
# NBI
```{r}
censo2010 = st_read("data/informacion_censal_por_radio_2010_wgs84.geojson") %>% 
  st_transform(22185)%>% 
  st_make_valid() %>% 
  clean_names() %>% 
  mutate(hogares_con_nbi_pct = as.numeric(h_con_nbi)/as.numeric(t_hogar),
         t_hogar = as.numeric(t_hogar))


# Interpolación areal ponderada por área
radios_2022_con_nbi = aw_interpolate(
  .data      = radios_tot,
  tid        = cod_indec,
  source     = censo2010,
  sid        = id,
  weight     = "sum",
  output     = "sf",
  intensive  = "hogares_con_nbi_pct",
  extensive  = "t_hogar"
)

nbi_sector = radios_2022_con_nbi %>%
  st_drop_geometry() %>%
  group_by(sector) %>%
  summarise(
    nbi_pct = round(weighted.mean(hogares_con_nbi_pct, w = t_hogar, na.rm = TRUE),digits = 4)
  ) %>% 
  mutate(nombre_sector = if_else(sector == "0", "Barrio Mugica", paste0("Sector Próximo ", sector)))

rm(censo2010,radios_2022_con_nbi)
```
</details>

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
tabla = datatable(
  nbi_sector %>% 
    select(nombre_sector,nbi_pct) %>% 
    rename("Nombre Sector" = nombre_sector,
           "NBI Porcentual" = nbi_pct), 
  style = 'bootstrap', 
  class = 'cell-border stripe',
  options = list(
    searching = FALSE,
    rownames = FALSE,
    pageLength = 6,
    dom = 't',
    scrollY = '200px',
    language = list(zeroRecords = "No existen datos para los requerimientos seleccionados"),
    columnDefs = list(
      list(targets = 0, visible = FALSE),
      list(targets = c(1,2), width = '150px', className = 'dt-left')
    )
  )
)

tabla
```



<small style="color: #a64d79;"> [Se utilizó el NBI del Censo 2010 como indicador de privación estructural histórica del área, dado que el Censo 2022 no publicó aún datos de NBI desagregados a nivel de radio censal. Dado que la segregación urbana en CABA muestra alta persistencia temporal, especialmente en asentamientos informales consolidados, se asume que el NBI 2010 refleja adecuadamente las condiciones estructurales del área de estudio.
  Si el NBI 2010 ya mostraba una brecha estructural pronunciada, y la evidencia sugiere que la pandemia profundizó la desigualdad urbana en Argentina, el uso del NBI 2010 representa una estimación conservadora de la privación actual del sector informal.
  Fuentes para justificar: 
    * La crisis económica asociada a la emergencia sanitaria impactó particularmente en los barrios informales, en los que los índices de informalidad laboral y cuentapropismo son generalmente más altos, lo que profundiza las dificultades vinculadas al confinamiento prolongado. INDEC
https://www.cippec.org/publicacion/covid-barrio-mugica-evaluacion-impacto/
    * Según el ODSA, luego de la crisis económico-sanitaria, en plena recuperación económica de 2022, el porcentaje de población en situación de pobreza ya había alcanzado el 40%, lo que en lo que va del siglo XXI representa un crecimiento de al menos 15 puntos porcentuales. Indec
El informe completo está acá:
https://wadmin.uca.edu.ar/public/ckeditor/Observatorio%20Deuda%20Social/Documentos/2022/2022-OBSERVATORIO-RESUMEN-Deudas-Sociales-en-la-Argentina-Urbana-2010-2022_Anexo-PRENSA.pdf
    * La CEPAL estimó que la pobreza en Argentina trepó a 38,8% en 2020 como consecuencia de la pandemia, representando un retroceso de 12 años en términos de pobreza. INDEC
El informe de referencia es el Panorama Social de América Latina 2020:
https://www.cepal.org/es/comunicados/pandemia-provoca-aumento-niveles-pobreza-sin-precedentes-ultimas-decadas-impacta] </small>

<h2>
  SUB-CANASTA </br>
  <small class="text-muted"> |  Productos relevados por establecimiento </small>
</h2>

Para estudiar el costo de los productos alimentarios a los cuales tienen acceso los hogares de cada sector, se relevaron los comercios comprendidos dentro de las isocronas. 
En base a esto se pudo extraer una sub-canasta alimentaria.

Para definir qué alimentos relevar, se tomó la canasta básica alimentaria, y se seleccionaron 7 productos en base a los siguientes criterios:
  1) Representatividad: Productos de consumo proporcionalmente elevado dentro de los valores definidos para un adulto (ver tabla de referencia), de categorías diversas, y que permitirían elaborar las 4 comidas. (sin incluir frutas ni hortalizas)
  2) Homogeneidad: Siempre se compararon las mismas unidades de venta. 
A falta de poder mantener constante la marca/calidad de los mismos, se seleccionó el artículo de cada categoría con el precio más económico disponible en cada local.

Los relevamientos se realizaron siguiendo las preguntas del <em>Anexo I - Formulario de relevamiento</em> resultando en las tablas del <em>Anexo II - Relevamiento territorial Barrio Mugica</em> y el <em>Anexo III - Relevamiento territorial Sectores Formales Próximos</em>

<small style="color: #a64d79;"> [ Armar las tablas en Rmds separados para facilitar acceso y visualización] </small>


<details><summary> Código </summary>
## PRODUCTOS
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
#TRANSFORMO LA BASE DE COMERCIOS EN BASE DE PRODUCTOS DESGLOSADOS.

productos_tot = comercios_tot %>% 
  select(id_comercio,rubro,leche,harina,azucar,arroz,papa,fiambre_jc,carne_pic,cadena,tanda) %>%  
  pivot_longer(
    cols = c(leche, harina, azucar, arroz, papa, fiambre_jc, carne_pic),
    names_to  = "producto",
    values_to = "precio"
  ) %>% 
  mutate(
    producto_id = paste(producto, id_comercio, sep = "_"),
    precio = round(precio, digits=2),
    formal = as.factor(if_else(grepl("cbm",id_comercio),0,1))
  ) %>% 
  select(producto_id,producto, precio, id_comercio, rubro, cadena, tanda,formal, geometry) %>% 
  filter(!is.na(precio)) # saca los NA (combos donde ese local no tiene ese producto

```
</details>



```{r, echo=FALSE, out.width='90%', fig.align='left', out.height=400}

ggplot(productos_tot, aes(x = precio, y = producto, fill = formal)) +
  geom_boxplot(outlier.shape = NA, alpha = 0.8) +
  scale_fill_brewer(palette = "Accent") +
  labs(x = "Precios", y = "Producto", fill = "Sector Formal") +
  theme_minimal(base_size = 14) +
  theme_minimal(base_size = 14) +
  scale_x_continuous(limits = c(0, 22000), breaks = seq(0, 22000, by = 2000))+
  theme(
    legend.position = "bottom",
    plot.title = element_blank(),
    axis.text.y = element_text(size = 10)
  )
```

<details><summary> Código </summary>

#CANASTAS MIN Y MAX POR PUNTO

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

#DEFINO UNA FUNCION PARA CALCULAR CANASTAS MINIMAS Y MAXIMAS DISPONIBLES POR PUNTO
calcular_canasta = function(df, tipo = c("min", "max")) {
  tipo = match.arg(tipo)
  slice_fn = if (tipo == "min") slice_min else slice_max
  
  todos_productos <- c("leche", "harina", "azucar", "arroz", "papa", "fiambre_jc", "carne_pic")
  
  df %>%
    st_drop_geometry() %>% 
    filter(!is.na(id_punto)) %>%
    group_by(id_punto, producto) %>%
    slice_fn(precio, n = 1, with_ties = FALSE) %>%
    ungroup() %>%
    group_by(id_punto) %>%
    summarise(
      "precio_canasta_{tipo}" := sum(precio),
      n_locales_necesarios     = n_distinct(id_comercio),
      prod_faltante            = {
        presentes <- unique(producto)
        faltantes <- setdiff(todos_productos, presentes)
        if (length(faltantes) == 0) NA_character_ else paste(faltantes, collapse = ", ")},
      .groups = "drop"
    )
}

#DEFINO UNA FUNCION PARA CALULAR MARGENES ENTRE CANASTAS P5 Y P95 PARA EVITAR OUTLIERS

calcular_margen_canasta = function(df) {
  df %>%
    st_drop_geometry() %>%
    filter(!is.na(id_punto)) %>%
    group_by(id_punto, producto) %>%
    summarise(
      p5  = quantile(precio, 0.05),
      p95 = quantile(precio, 0.95),
      n_locales = n_distinct(id_comercio),
      .groups = "drop"
    ) %>%
    group_by(id_punto) %>%
    summarise(
      # Minimos en p5
      canasta_sum_p5  = sum(p5,  na.rm = TRUE), 
      canasta_gm_p5   = exp(mean(log(p5),  na.rm = TRUE)), 
      # Máximos en p95
      canasta_sum_p95  = sum(p95, na.rm = TRUE),
      canasta_gm_p95   = exp(mean(log(p95), na.rm = TRUE)),
      n_productos      = n(),
      productos_faltantes = 7 - n(),
      .groups = "drop"
    ) %>%
    mutate(
      # diferencia absoluta:
      margen_absoluto_p     = canasta_sum_p95 - canasta_sum_p5, 
      # diferencia relativa (%) margen como proporción del mínimo:
      margen_relativo_pct_p = margen_absoluto_p / canasta_sum_p5,
      # brecha geométrica (%) igual que arriba, pero usando media geométrica:
      margen_gm_pct_p  = canasta_gm_p95 / canasta_gm_p5 - 1,
      # logaritmo de la razón entre p5 y p95 (útil para modelar):
      margen_log_p     = log(canasta_gm_p95) - log(canasta_gm_p5),
      formal         = ifelse(str_starts(id_punto, "0"), 0, 1)
    )
}



#TOT_05--------------------------------------------------------------------------------

#FILTRO LOS PRODUCTOS/COMERCIOS QUE QUEDAN DENTRO DE CADA ISOCRONA
comercios_en_isocrona05_tot = st_join(
  productos_tot,    # puntos (comercios)
  isocronas05_tot,         # polígonos (uno por habitante)
  join = st_within
)

#CALCULO PARA ISOCRONAS DE 5MIN

resumen_canasta_min <- calcular_canasta(comercios_en_isocrona05_tot, "min")
resumen_canasta_max <- calcular_canasta(comercios_en_isocrona05_tot, "max")
margen_canastas <- calcular_margen_canasta(comercios_en_isocrona05_tot)

#HAGO UN RESUMEN POR PUNTO: Cuanto sale la canasta más barata, Cuantos productos cubre, A cuántos lugares distintos tengo que ir para conseguirla?
resumen_canasta05_tot = left_join(resumen_canasta_min, 
                                  resumen_canasta_max %>% 
                                   select(id_punto, precio_canasta_max),
                                by = "id_punto")

resumen_canasta05_tot = left_join(margen_canastas,resumen_canasta05_tot,
                                           by="id_punto")

resumen_canasta05_tot = puntos_aleatorios_tot %>% 
  select(id_punto)%>%
  left_join(resumen_canasta05_tot, by = "id_punto")%>% 
  mutate(margen_absoluto = precio_canasta_max-precio_canasta_min)


#TOT_10------------------------------------------------------

#FILTRO LOS PRODUCTOS/COMERCIOS QUE QUEDAN DENTRO DE CADA ISOCRONA
comercios_en_isocrona10_tot = st_join(
  productos_tot,    # puntos (comercios)
  isocronas10_tot,         # polígonos (uno por habitante)
  join = st_within
)

#CALCULO PARA ISOCRONAS DE 10MIN

resumen_canasta_min <- calcular_canasta(comercios_en_isocrona10_tot, "min")
resumen_canasta_max <- calcular_canasta(comercios_en_isocrona10_tot, "max")
margen_canastas <- calcular_margen_canasta(comercios_en_isocrona10_tot)

#HAGO UN RESUMEN POR PUNTO: Cuanto sale la canasta más barata, Cuantos productos cubre, A cuántos lugares distintos tengo que ir para conseguirla?
resumen_canasta10_tot <- left_join(resumen_canasta_min, 
                                resumen_canasta_max %>% select(id_punto, precio_canasta_max),
                                by = "id_punto") 

resumen_canasta10_tot = left_join(margen_canastas,resumen_canasta10_tot, by="id_punto")

resumen_canasta10_tot = puntos_aleatorios_tot %>% 
  select(id_punto)%>%
  left_join(resumen_canasta10_tot, by = "id_punto")%>% 
  mutate(margen_absoluto = precio_canasta_max-precio_canasta_min)


rm(resumen_canasta_min, resumen_canasta_max, margen_canastas)
```


```{r}
#CANASTA 05
sumtab <- resumen_canasta05_tot %>%
  filter(productos_faltantes==0) %>% 
  group_by(formal) %>%
  summarise(
    n_obs          = n(),
    # GM “cara”
    mean_gm_p95    = mean(canasta_gm_p95, na.rm = TRUE),
    median_gm_p95  = median(canasta_gm_p95, na.rm = TRUE),
    wmean_gm_p95   = weighted.mean(canasta_gm_p95, n_productos, na.rm = TRUE),
    # Brecha (log)
    mean_head_log  = mean(margen_log_p, na.rm = TRUE),
    median_headpct = median(margen_gm_pct_p, na.rm = TRUE),
    .groups = "drop"
  )
print(sumtab)
```

```{r}
#CANASTA 10
sumtab <- resumen_canasta10_tot %>%
  group_by(formal) %>%
  summarise(
    n_obs          = n(),
    # GM “cara”
    mean_gm_p95    = mean(canasta_gm_p95, na.rm = TRUE),
    median_gm_p95  = median(canasta_gm_p95, na.rm = TRUE),
    wmean_gm_p95   = weighted.mean(canasta_gm_p95, n_productos, na.rm = TRUE),
    # Brecha (log)
    mean_head_log  = mean(margen_log_p, na.rm = TRUE),
    median_headpct = median(margen_gm_pct_p, na.rm = TRUE),
    .groups = "drop"
  )
print(sumtab)
```

```{r}
#CANASTA 05
# Asegura que el grupo sea factor con niveles y etiquetas claras
resumen_canasta05_tot <- resumen_canasta05_tot %>%
  mutate(grupo = factor(ifelse(formal == 1, "formal", "informal"),
                        levels = c("formal","informal")))

# t-tests
t_gmmax <- t.test(log(canasta_gm_p95) ~ grupo, data = resumen_canasta05_tot)   # WTP (cola alta)
t_head  <- t.test(margen_log_p       ~ grupo, data = resumen_canasta05_tot)    # brecha interna

# Convierte diferencia en logs -> % y orienta a (slum - formal)
conv_pct <- function(tt){
  nm <- tolower(names(tt$estimate))
  m_formal   <- unname(tt$estimate[nm == "mean in group formal"])
  m_informal <- unname(tt$estimate[nm == "mean in group informal"])
  diff_log <- m_informal - m_formal                 # informal − formal
  ci_log   <- sort(-tt$conf.int)                    # pasa a (informal − formal)
  c(
    pct_diff = 100*(exp(diff_log) - 1),
    ci_low   = 100*(exp(ci_log[1]) - 1),
    ci_high  = 100*(exp(ci_log[2]) - 1),
    p_value  = unname(tt$p.value)
  )
}

res_gmmax <- conv_pct(t_gmmax)   # % dif en GM de canasta “cara” (slum − formal)
res_head  <- conv_pct(t_head)    # % dif en margen (slum − formal)

res_gmmax
res_head

# Robustez no paramétrica
wil_gmmax <- wilcox.test(log(canasta_gm_p95) ~ grupo, data = resumen_canasta05_tot, exact = FALSE)
wil_head  <- wilcox.test(margen_log_p       ~ grupo, data = resumen_canasta05_tot, exact = FALSE)
wil_gmmax$p.value; wil_head$p.value
```
```{r}
#CANASTA 10
# Asegura que el grupo sea factor con niveles y etiquetas claras
resumen_canasta10_tot <- resumen_canasta10_tot %>%
  mutate(grupo = factor(ifelse(formal == 1, "formal", "informal"),
                        levels = c("formal","informal")))

# t-tests
t_gmmax <- t.test(log(canasta_gm_p95) ~ grupo, data = resumen_canasta10_tot)   # WTP (cola alta)
t_head  <- t.test(margen_log_p       ~ grupo, data = resumen_canasta10_tot)    # brecha interna

# Convierte diferencia en logs -> % y orienta a (slum - formal)
conv_pct <- function(tt){
  nm <- tolower(names(tt$estimate))
  m_formal   <- unname(tt$estimate[nm == "mean in group formal"])
  m_informal <- unname(tt$estimate[nm == "mean in group informal"])
  diff_log <- m_informal - m_formal                 # informal − formal
  ci_log   <- sort(-tt$conf.int)                    # pasa a (informal − formal)
  c(
    pct_diff = 100*(exp(diff_log) - 1),
    ci_low   = 100*(exp(ci_log[1]) - 1),
    ci_high  = 100*(exp(ci_log[2]) - 1),
    p_value  = unname(tt$p.value)
  )
}

res_gmmax <- conv_pct(t_gmmax)   # % dif en GM de canasta “cara” (slum − formal)
res_head  <- conv_pct(t_head)    # % dif en margen (slum − formal)

res_gmmax
res_head

# Robustez no paramétrica
wil_gmmax <- wilcox.test(log(canasta_gm_p95) ~ grupo, data = resumen_canasta10_tot, exact = FALSE)
wil_head  <- wilcox.test(margen_log_p       ~ grupo, data = resumen_canasta10_tot, exact = FALSE)
wil_gmmax$p.value; wil_head$p.value
```

La brecha entre lo más caro disponible y lo más barato disponible es mayor en entornos informales? Esto implicaría que los habitantes informales no solo pagan más sino que tienen menos capacidad de arbitrar entre opciones.

```{r}
# CANASTA 05
# Comparación de márgenes entre entornos formal e informal
resumen_canasta05_tot %>%
  st_drop_geometry() %>% 
  group_by(formal) %>%
  summarise(
    # Brecha absoluta promedio
    margen_sum_medio    = mean(margen_absoluto_p, na.rm = TRUE),
    # Brecha relativa promedio (% sobre el mínimo)
    margen_sum_pct_medio = mean(margen_relativo_pct_p, na.rm = TRUE),
    # Brecha geométrica promedio
    margen_gm_pct_medio  = mean(margen_relativo_pct_p, na.rm = TRUE),
    # Canasta mínima promedio (para contextualizar)
    canasta_min_media   = mean(canasta_sum_p5, na.rm = TRUE),
    # Canasta máxima promedio
    canasta_max_media   = mean(canasta_sum_p95, na.rm = TRUE),
    n_puntos            = n(),
    .groups = "drop"
  )
```

```{r}
# CANASTA 10
# Comparación de márgenes entre entornos formal e informal
resumen_canasta10_tot %>%
  st_drop_geometry() %>% 
  group_by(formal) %>%
  summarise(
    # Brecha absoluta promedio
    margen_sum_medio    = mean(margen_absoluto_p, na.rm = TRUE),
    # Brecha relativa promedio (% sobre el mínimo)
    margen_sum_pct_medio = mean(margen_relativo_pct_p, na.rm = TRUE),
    # Brecha geométrica promedio
    margen_gm_pct_medio  = mean(margen_relativo_pct_p, na.rm = TRUE),
    # Canasta mínima promedio (para contextualizar)
    canasta_min_media   = mean(canasta_sum_p5, na.rm = TRUE),
    # Canasta máxima promedio
    canasta_max_media   = mean(canasta_sum_p95, na.rm = TRUE),
    n_puntos            = n(),
    .groups = "drop"
  )
```

El indicador más relevante para mi pregunta es margen_sum_pct_medio (brecha relativa), porque la brecha absoluta puede ser mayor en entornos formales simplemente porque los precios son más altos en términos nominales. La brecha relativa me dice si proporcionalmente los informales tienen menos capacidad de encontrar opciones económicas.
Para complementar, se realiza un test estadístico simple:

```{r}
# CANASTA 05
# Test de diferencia de medias
t.test(margen_relativo_pct_p ~ formal, data = resumen_canasta05_tot)

# O con Wilcoxon si la distribución no es normal
wilcox.test(margen_relativo_pct_p ~ formal, data = resumen_canasta05_tot)
```

```{r}
# CANASTA 10
# Test de diferencia de medias
t.test(margen_relativo_pct_p ~ formal, data = resumen_canasta10_tot)

# O con Wilcoxon si la distribución no es normal
wilcox.test(margen_relativo_pct_p ~ formal, data = resumen_canasta10_tot)
```

Para fortalecer el argumento se normaliza el margen por el nivel de precios del entorno, para asegurar que la diferencia no sea simplemente consecuencia de que los precios absolutos son más altos o más bajos en un contexto:

```{r}
#CANASTA 05
resumen_canasta05_tot |>
  group_by(formal) |>
  summarise(
    margen_relativo_medio = mean(margen_relativo_pct_p, na.rm = TRUE),
    canasta_min_media     = mean(canasta_sum_p5, na.rm = TRUE),
    canasta_max_media     = mean(canasta_sum_p95, na.rm = TRUE),
    n = n()
  )
```
```{r}
#CANASTA 10
resumen_canasta10_tot |>
  group_by(formal) |>
  summarise(
    margen_relativo_medio = mean(margen_relativo_pct_p, na.rm = TRUE),
    canasta_min_media     = mean(canasta_sum_p5, na.rm = TRUE),
    canasta_max_media     = mean(canasta_sum_p95, na.rm = TRUE),
    n = n()
  )
```

```{r}
# Distribución del margen por grupo CANASTA 05
ggplot(resumen_canasta05_tot, aes(x = factor(formal), y = margen_relativo_pct_p, fill = factor(formal))) +
  geom_boxplot(alpha = 0.6) +
  geom_jitter(width = 0.1, size = 2, alpha = 0.7) +
  scale_x_discrete(labels = c("0" = "Formal", "1" = "Informal")) +
  scale_fill_manual(values = c("0" = "#134f5c", "1" = "#f6b26b")) +
  labs(x = NULL, y = "Margen relativo (p95 - p5) / p5") +
  theme_minimal() +
  theme(legend.position = "none")
```

```{r}
# Distribución del margen por grupo CANASTA 10
ggplot(resumen_canasta10_tot, aes(x = factor(formal), y = margen_relativo_pct_p, fill = factor(formal))) +
  geom_boxplot(alpha = 0.6) +
  geom_jitter(width = 0.1, size = 2, alpha = 0.7) +
  scale_x_discrete(labels = c("0" = "Formal", "1" = "Informal")) +
  scale_fill_manual(values = c("0" = "#134f5c", "1" = "#f6b26b")) +
  labs(x = NULL, y = "Margen relativo (p95 - p5) / p5") +
  theme_minimal() +
  theme(legend.position = "none")
```

Lo que muestran los datos
El boxplot lo dice claramente: el entorno informal tiene mayor dispersión y mayor margen relativo, con la mediana informal (~0.55) casi el doble que la formal (~0.30). Pero la caja informal es mucho más ancha, lo que explica por qué el Wilcoxon no es significativo — hay mucha varianza interna en el grupo informal.
Sobre la discrepancia t-test vs Wilcoxon
Mirando el gráfico, la causa es evidente: el grupo informal tiene outliers hacia arriba (valores de 2, 2.5, 3.5) que inflan la media pero no la mediana. El t-test es sensible a eso, el Wilcoxon no. Los resultados más robustos de tu análisis anterior con margen_log (donde ambos tests fueron significativos) son más confiables precisamente porque la escala logarítmica comprime esos outliers.

Conclusión metodológica:
Los resultados con margen_log (ambos tests significativos, p = 0.005 y p = 0.034) son el argumento más sólido. El margen_sum_pct en escala lineal tiene la misma dirección pero es más sensible a outliers. Es importante tener ambos en cuenta, considerando que la robustez del resultado en escala logarítmica respalda la hipótesis del impuesto a la pobreza relativo.

</details>

Imagen 8. Precios mínimos y máximos de sub-canastas a las que se puede acceder desde cada punto a una distancia a pie de hasta 5 minutos.

  El mapa incluye información de:
  
  * Precios máximos y mínimos.
  * Cantidad de productos que se consiguen (el máximo es 7, corresponde a sub-canasta completa)
  * Cantidad de productos que faltan para completar la sub-canasta.
  * Cantidad de locales diferentes que hace falta alcanzar para obtener ese valor (min o max.)
  * Diferencia de precio entre ambas sub-canastas (min y max.)
    
    
<small style="color: #a64d79;"> [ Considerar si acá sería interesante mostrar las isocronas también o es demasiada info. Capaz conviene sacar los puntos de los locales? ] </small>

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

transformar_todo_a_crs(4326)

mapa =
leaflet(st_zm(sectores_tot)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("Barrio Mugica", "Oferta a 5min por punto", "Sectores Proximos", "Comercios"))%>% 
  


  addPolygons(data= b_mugica,
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Barrio Mugica") %>%
  
  addPolygons(data= sectores_tot,
              color = "black",
              weight = 0.6,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Sectores Proximos") %>%
  
    addCircleMarkers(data = comercios_tot,
              fillColor = "#f6b26b",
              weight = 0.5,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              group = "Comercios") %>% 

  
  addCircleMarkers(data=resumen_canasta05_tot,
              fillColor = "black",
              weight = 4,
              color = "black",
              fillOpacity = 0.5,
              popup = ~paste("<strong>ID Punto:</strong>", id_punto,
                             "</br> <strong>Sub-canasta Min:</strong>", precio_canasta_min,
                             "</br> <strong>Sub-canasta Max:</strong>", precio_canasta_max,
                             "</br> <strong>Productos Faltantes:</strong>", productos_faltantes,
                             "</br> <strong>N Locales:</strong>", n_locales_necesarios),
              radius = 2,
              group = "Oferta a 5min por punto")

 
mapa

transformar_todo_a_crs(22185)
```

Imagen 9. Precios mínimos y máximos de sub-canastas a las que se puede acceder desde cada punto a una distancia a pie de hasta 10 minutos.

(El mapa contiene los mismos tipos de información que el anterior, pero en este caso se visualiza teniendo en cuenta locales comprendidos dentro de las isocronas de 10 minutos de distancia a pie desde cada punto)
    
    
<small style="color: #a64d79;"> [ Considerar si acá sería interesante mostrar las isocronas también o es demasiada info. Capaz conviene sacar los puntos de los locales? ] </small>

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

transformar_todo_a_crs(4326)

mapa =
leaflet(st_zm(sectores_tot)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("Barrio Mugica", "Oferta a 10min por punto", "Sectores Proximos", "Comercios"))%>% 
  


  addPolygons(data= b_mugica,
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Barrio Mugica") %>%
  
  addPolygons(data= sectores_tot,
              color = "black",
              weight = 0.6,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Sectores Proximos") %>%
  
    addCircleMarkers(data = comercios_tot,
              fillColor = "#f6b26b",
              weight = 0.5,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              group = "Comercios") %>% 

  
  addCircleMarkers(data=resumen_canasta10_tot,
              fillColor = "black",
              weight = 4,
              color = "black",
              fillOpacity = 0.5,
              popup = ~paste("<strong>ID Punto:</strong>", id_punto,
                             "</br> <strong>Sub-canasta Min:</strong>", precio_canasta_min,
                             "</br> <strong>Sub-canasta Max:</strong>", precio_canasta_max,
                             "</br> <strong>Productos Faltantes:</strong>", productos_faltantes,
                             "</br> <strong>N Locales:</strong>", n_locales_necesarios),
              radius = 2,
              group = "Oferta a 10min por punto") 
 
mapa

transformar_todo_a_crs(22185)
```

<details><summary> Código </summary>
# REVISAR SI USO ESTO O NO...

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
# Función que dado un id_punto, encuentra el par de locales 
# que minimiza el precio total de la canasta
mejor_par_locales <- function(df_punto) {
  
  # Locales disponibles en esta isocrona
  locales <- unique(df_punto$id_comercio)
  
  # Si hay 0 o 1 local, no hay par que evaluar
  if (length(locales) < 2) {
    # Igual devolvemos lo que hay con 1 local
    resultado <- df_punto |>
      group_by(producto) |>
      slice_min(precio, n = 1, with_ties = FALSE) |>
      ungroup()
    return(list(
      precio_canasta       = sum(resultado$precio),
      n_productos_cubiertos = nrow(resultado),
      n_locales_necesarios  = n_distinct(resultado$id_comercio),
      productos_faltantes   = 7 - nrow(resultado)
    ))
  }
  
  # Generar todas las combinaciones de 2 locales
  pares <- combn(locales, 2, simplify = FALSE)
  
  # Para cada par, calcular el precio mínimo de la canasta
  evaluar_par <- function(par) {
    df_punto |>
      filter(id_comercio %in% par) |>
      group_by(producto) |>
      slice_min(precio, n = 1, with_ties = FALSE) |>
      ungroup() |>
      summarise(
        precio_canasta        = sum(precio),
        n_productos_cubiertos = n(),
        n_locales_necesarios  = n_distinct(id_comercio),
        productos_faltantes   = 7 - n()
      )
  }
  
  resultados_pares <- map_dfr(pares, evaluar_par)
  
  # Quedarse con el par que minimiza el precio
  resultados_pares |>
    slice_min(precio_canasta, n = 1, with_ties = FALSE)
}

# Aplicar por cada id_punto
#resumen_2locales_05 <- comercios_en_isocrona |>
#  filter(!is.na(id_punto), !is.na(precio)) |>
#  group_by(id_punto) |>
#  group_modify(~ mejor_par_locales(.x)) |>
#  ungroup() |>
#  mutate(isocrona = "05min")


# Combinar
#resumen_2locales <- bind_rows(resumen_2locales_05, resumen_2locales_10)
```

</details>


<small style="color: #a64d79;"> [ VOLVER A ESTA PARTE CUANDO TENGA TODOS LOS DATOS. REVISAR METODO DE ANÁLISIS Y CONCLUSIONES. ] </small>

Comercios por isocrona

<details><summary> Código </summary>
## AHORA LA BORRO, RECUPERAR DESPUES!!!
</details>

<h2>
  OTRAS VARIABLES </br>
  <small class="text-muted"> |  Análisis de variables urbanas </small>
</h2>

Con la intención de encontrar relaciones que puedan ser causales entre factores urbanos y la condición de la oferta en los sectores formales y el informal, se analizan:

  * La irrigación de cada sector, definida como la cantidad de puntos de acceso y las características de las vías públicas comprendidas dentro de los territorios.
  * Accesibilidad medios de transporte?
  * Seguridad expresada en crimen?
  
  <small style="color: #a64d79;"> [ Expresar esto más piola y decidir qué variables quiero/puedo incluir en el análisis teniendo en cuenta las bases de datos disponibles y la representatividad (?) de las mismas... Por ejemplo crimen me interesa, pero no se como son los reportes de crimen en zonas informales, hay subdeclaración? Investigar ] </small>

<h2>
  IRRIGACIÓN </br>
  <small class="text-muted"> |  Accesos y calles comprendidas dentro de los sectores </small>
</h2>

Intersectando el callejero de la ciudad con los polígonos del Barrio Mugica y de los sectores próximos se calcula la irrigación vial al polígono en términos de puntos de acceso.

<details><summary> Código </summary>
## IRRIGACION
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
#IMPORTO EL CALLEJERO:
callejero = st_read("data/calles.shp") %>% 
  st_transform(22185)


#GENERO PUNTOS EN LAS INTERSECCIONES DE LAS CALLES CON LOS POLIGONOS DE LOS SECTORES PARA CALCULAR ACCESIBILIDAD

# This gives you actual POINT geometries where the lines intersect polygon boundaries
interseccion_callejero_bm = st_intersection(
  st_cast(callejero, "LINESTRING"),
  st_boundary(b_mugica) %>% 
    st_transform(22185)) %>%
  filter(st_geometry_type(.) == "POINT")  # ensures only POINT geometries

interseccion_callejero_sp <- st_intersection(
  st_cast(callejero, "LINESTRING"),
  st_boundary(sectores_tot) %>% 
    st_transform(22185)) %>%
  filter(st_geometry_type(.) == "POINT")  # ensures only POINT geometries


# Then count per line if needed
n_interseccion_callejero_bm = interseccion_callejero_bm %>%
  st_drop_geometry() %>%
  count()

n_interseccion_callejero_sp <- interseccion_callejero_sp %>%
  st_drop_geometry() %>%
  group_by(sector) %>%
  summarise(n_intersecciones = n())


```

# Intersecciones internas
```{r}
# Calles dentro del Barrio Mugica
calles_bm = st_intersection(
  st_cast(callejero, "LINESTRING"),
  st_transform(b_mugica, 22185)
) %>%
  filter(st_geometry_type(.) %in% c("LINESTRING", "MULTILINESTRING"))

# Calles dentro de los sectores formales
calles_sp = st_intersection(
  st_cast(callejero, "LINESTRING"),
  st_transform(sectores_tot, 22185)
) %>%
  filter(st_geometry_type(.) %in% c("LINESTRING", "MULTILINESTRING"))

intersecciones_internas_bm = st_intersection(calles_bm) %>%
  filter(st_geometry_type(.) == "POINT") %>% 
  mutate(sector = 0) %>% 
  select(sector) %>% 
  st_transform(22185)

intersecciones_internas_sp = st_intersection(calles_sp) %>%
  filter(st_geometry_type(.) == "POINT") %>% 
  select(sector) %>% 
  st_transform(22185)

intersecciones_internas_tot = rbind(intersecciones_internas_bm,intersecciones_internas_sp) %>% 
  group_by(sector) %>% 
  summarise(n_intersecciones_internas = n()) %>% 
  left_join(sectores_tot %>% 
              st_drop_geometry()) %>% 
  mutate(ratio_intersecciones_internas_km2 = round(n_intersecciones_internas/area_km2_s, 2))
  
```

</details>

Imagen 10. Puntos de acceso por polígono.

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

transformar_todo_a_crs(4326)

mapa =
leaflet(st_zm(sectores_tot)) %>%
  addTiles() %>%
  addProviderTiles(providers$CartoDB.Positron, group = "OSM",
                   options = providerTileOptions(minzoom = 1, maxzoom = 15)) %>%
  addProviderTiles(providers$Esri.WorldImagery, group = "Satelite") %>%
  addLayersControl(
    baseGroups = c("OSM","Satelite"), 
    overlayGroups = c("Barrio Mugica","Accesos BM", "Sectores Próximos", "Accesos SP 1", "Accesos SP 2", "Accesos SP 3", "Accesos SP 4", "Accesos SP 5", "Intersecciones internas SP", "Intersecciones internas BM"))%>% 
  


  addPolygons(data= b_mugica,
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Barrio Mugica") %>%
  
   addCircleMarkers(data = interseccion_callejero_bm,
              fillColor = "#f6b26b",
              weight = 2,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              group = "Accesos BM") %>% 
  

      addPolygons(data= sectores_tot,
              color = "black",
              weight = 1,
              opacity = 0.7,
              fillColor = "white",
              fillOpacity = 0,
              group = "Sectores Próximos") %>% 
  

  addCircleMarkers(data = interseccion_callejero_sp%>% filter(sector=="1"),
              fillColor = "#f6b26b",
              weight = 2,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              group = "Accesos SP 1") %>% 
  

  addCircleMarkers(data = interseccion_callejero_sp%>% filter(sector=="2"),
              fillColor = "#f6b26b",
              weight = 2,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              group = "Accesos SP 2") %>% 
  

  addCircleMarkers(data = interseccion_callejero_sp%>% filter(sector=="3"),
              fillColor = "#f6b26b",
              weight = 2,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              group = "Accesos SP 3") %>% 
  

  addCircleMarkers(data = interseccion_callejero_sp%>% filter(sector=="4"),
              fillColor = "#f6b26b",
              weight = 2,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              group = "Accesos SP 4") %>% 
  

  addCircleMarkers(data = interseccion_callejero_sp%>% filter(sector=="5"),
              fillColor = "#f6b26b",
              weight = 2,
              color = "#f6b26b",
              fillOpacity = 1,
              radius = 2,
              group = "Accesos SP 5") %>% 
  
  addCircleMarkers(data= intersecciones_internas_sp,
              color = "#134f5c",
              weight = 1,
              opacity = 0.8,
              fillColor = "#134f5c",
              fillOpacity = 0.8,
              radius = 1,
              group = "Intersecciones Internas SP") %>%  
  
  addCircleMarkers(data= intersecciones_internas_bm,
              color = "#134f5c",
              weight = 1,
              opacity = 0.8,
              fillColor = "#134f5c",
              fillOpacity = 0.8,
              radius = 1,
              group = "Intersecciones Internas BM")

mapa

transformar_todo_a_crs(22185)
```


<details><summary> Código </summary>
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
n_interseccion_callejero_bm = n_interseccion_callejero_bm %>% 
  mutate(sector = 0) %>% 
  rename("Cantidad de Puntos de Acceso" = n)

n_interseccion_callejero_sp = n_interseccion_callejero_sp %>% 
  rename("Cantidad de Puntos de Acceso" = n_intersecciones)

intersecciones_tabla = rbind(n_interseccion_callejero_sp, n_interseccion_callejero_bm)

intersecciones_tabla = left_join(intersecciones_tabla, intersecciones_internas_tot, by="sector")%>% 
  mutate(nombre_sector = if_else(sector == "0", "Barrio Mugica", paste0("Sector Próximo ", sector))) %>% 
  select(nombre_sector,"Cantidad de Puntos de Acceso",n_intersecciones_internas,ratio_intersecciones_internas_km2,viviendas, densidad_viviendas) %>% 
  rename("Nombre Sector" = nombre_sector,
         "N Intersecciones Internas" = n_intersecciones_internas,
         "Ratio Intersecciones Internas/km2" = ratio_intersecciones_internas_km2,
         "N Viviendas" = viviendas,
         "Densidad Viviendas" = densidad_viviendas) %>% 
  st_drop_geometry()
```
</details>

Tabla 1. Cantidad de puntos de acceso por sector.

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
tabla =
datatable(intersecciones_tabla, 
  style = 'bootstrap', 
  class = 'cell-border stripe',
  options = list(
    searching = FALSE,
    rownames = FALSE,
    pageLength = 5,
    dom = 't',
    scrollY = '200px',
    language = list(zeroRecords = "No existen datos para los requerimientos seleccionados"),
    columnDefs = list(
      list(targets = 0, visible = FALSE), # hide column 0
      list(targets = c(1,2), width = '150px', className = 'dt-left'))
    )
  )

tabla

```

Tabla 2. Longitud de vías públicas dentro de cada sector por tipo.

  Tipos de vías públicas y definiciones:
  
  * Avenida: Vía pública de 20 m o más de ancho (CPU 1.2.1.2, GCBA).
  * Calle: Vía pública de entre 6 y 19,99 m de ancho (CPU 1.2.1.2, GCBA).
  * Pasaje: Vía pública de ancho reducido, generalmente menor a 6 m (algunos de uso peatonal o vehicular restringido). El Código Urbanístico vigente (Ley 6.764) regula específicamente los pasajes, estableciendo una altura máxima de 9 metros para construcciones en los más de 400 pasajes de la Ciudad. IADB
  * Pasillo: Ancho variable de 2 m a 1,35 m. No es calle oficial por Ley 6129

<details><summary> Código </summary>
```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}

#FILTRO LOS QUE SON ALMACENES:
callejero_bm = st_read("data/informacion_territorial.gpkg", layer = "calles") %>% 
  st_transform(22185) %>% 
  filter(!is.na(NOMBRE)) %>% 
  mutate(tipo_c = ifelse(grepl("^Calle ", NOMBRE), "CALLE", "PASAJE"),
         longitud_calle = length(geom)) %>% 
  rename(geometry = geom) %>% 
  group_by(tipo_c) %>% 
  summarise(longitud_tot = sum(longitud_calle),
            .groups = "drop") %>%
  mutate(tipo_ratio = longitud_tot / sum(longitud_tot),
         tipo_ratio = round(tipo_ratio,2),
         barrio = "Barrio Mugica",
         longitud_tot = longitud_tot)
  

#TIPO/ANCHO DE CALLES EN ST
callejero_sp = st_intersection(callejero, sectores_tot)  %>% 
  st_transform(22185) %>%  
  mutate(tipo_c = as.factor(tipo_c),
         longitud_calle = length(geometry)) %>% 
  group_by(sector, tipo_c) %>% 
  summarise(n_tipo = n(),
            longitud_tot = sum(longitud_calle),
            .groups = "drop") %>%
  group_by(sector) %>%
  mutate(tipo_ratio = longitud_tot / sum(longitud_tot),
         tipo_ratio = round(tipo_ratio,2),
         barrio = paste0("Sector Proximo ", sector),
         longitud_tot = longitud_tot) %>%
  ungroup()

tipo_calles = bind_rows(callejero_sp, callejero_bm) 

tipo_calles = tipo_calles %>% 
  select(-c(sector, n_tipo))%>% 
  mutate(tipo_c = str_to_title(tipo_c)) %>% 
  rename("Sector" = barrio,
         "Tipo de via" = tipo_c,
         "Longitud Total" = longitud_tot,
         "Ratio por tipo" = tipo_ratio) 

```
</details>

```{r, echo=FALSE, out.width='100%', fig.align='left', fig.height = 6}
tabla =
datatable(tipo_calles%>%  
            st_drop_geometry()%>%
            select(Sector, everything()), 
  style = 'bootstrap', 
  class = 'cell-border stripe',
  options = list(
    searching = FALSE,
    rownames = FALSE,
    pageLength = nrow(tipo_calles),   # ← show all rows
    dom = 't',                         # ← no search, no pagination, no length menu
    scrollY = FALSE,
    language = list(zeroRecords = "No existen datos para los requerimientos seleccionados"),
    columnDefs = list(
      list(targets = 0, visible = FALSE), # hide column 0
      list(targets = c(1:4), width = '150px', className = 'dt-left'))
    )
  )

tabla

```

<small style="color: #a64d79;"> [ Construir un indicador único que represente irrigación para poder incluir los valores a la regresión después ] </small>

<h2>
  SEGURIDAD </br>
  <small class="text-muted"> |  Delitos en 2023 </small>
</h2>


<details><summary> Código </summary>
```{r, out.width='100%', fig.align='left', fig.height = 6}

#LLAMO LA BASE DE DELITOS Y GUARDO SOLAMENTE LOS PUNTOS QUE ME INTERESAN PARA LLAMARLO LUEGO SIN TENER UNA BASE TAN GRANDE:
delitos = read_excel(
  "data/delitos_2023.xlsx",
  col_names = TRUE,
  col_types = NULL,
  na = "",
  trim_ws = TRUE,
  skip = 0) %>% 
  clean_names()%>%
  mutate(
    latitud = as.numeric(latitud),
    longitud = as.numeric(longitud),
    latitud = ifelse(abs(latitud) > 1000, latitud / 1e6, latitud),
    longitud = ifelse(abs(longitud) > 1000, longitud / 1e6, longitud)
  )

delitos_caba = delitos %>%
  filter(!is.na(latitud), !is.na(longitud), longitud!="NULL", latitud!="NULL", latitud!=0) %>% 
  st_as_sf(coords = c("longitud", "latitud"), crs = 4326)%>% 
  st_transform(22185)

  
delitos_sp = st_intersection(delitos_caba, isocronas10_tot) %>% 
  clean_names() %>% 
  mutate(tipo = as.factor(tipo))


delitos_bm = st_intersection(delitos_caba,isocronas10_tot)%>% 
  clean_names()%>% 
  mutate(tipo = as.factor(tipo))

#"Amenazas","Hurto","Lesiones","Robo","Vialidad"

rm(delitos,delitos_caba)

delitos_sp_n = delitos_sp %>% 
  group_by(id_punto,sector,tipo) %>% 
  summarise(cantidad=n())

delitos_bm_n = delitos_bm %>% 
  group_by(id_punto,tipo) %>% 
  summarise(cantidad=n())

```
</details>
